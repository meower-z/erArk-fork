# 按根因重组剩余本地修复(refactor-local-bugfixes-by-root-cause)

迁移自 openspec 同名 change(原文见 git 历史;删除时 114/160 任务完成,44 项未勾选,其中部分已陈旧)。这是一个**总盘子**,不是单一任务:把按历史症状组织的本地修复重组为按根因/状态生命周期/真实代码所有者划分的独立修复,每项走"线索—复现—根因—验证修复"的证据门禁(该门禁现固化在 `.codex/skills/investigate-game-bug`)。

## 2026-07-19 审计确认的有效剩余方向

- 高潮事务与寸止边缘状态的最终化(与 [[ADR-0003]] 的批结算契约衔接;上游 PR #221 已吸收一部分)。
- 统一 Tk/Web prompt 协议:Web 输入仍是进程全局变量(`Script/Core/web_server.py:102-105` 保存、`:2570-2603` 读后清空),没有 prompt ID、响应类型、客户端归属。
- 各修复的平台/人工证据补齐、禁用 mod 的迁移或退役决定、组合验证。

## 使用方式

不要按原 44 项清单顺序执行——先甄别哪些已被上游合并或已失效,再把仍有效的单项各开一个新 ticket。原 tasks.md 全文在 git 历史。
