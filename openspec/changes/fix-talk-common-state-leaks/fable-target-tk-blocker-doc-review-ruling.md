**REVISE**

依据：四份文档 + `Script/Design/talk.py:245-260,633-734`、`Script/Config/game_config.py:1431-1449`、`data/Talk_Common.json` type_id 全集、`data/csv/Behavior_Data.csv:481-482`。

## 必须修改的表述

1. **`target-scope-implementation-notes.md` 第 46 行开头："后续限定搜索确认 save 99 不能提供所需的正常 Tk 路线。"** 这是过度声明，且与同段结尾"在找到可正常到达的 NPC B、占位符和固定 seed 组合前"自相矛盾。已证事实只支持：首段文本不含占位符、一次 300 秒无界面结算超时未返回（结论不明，非否定证据）。改为"尚未找到/尚未证明"，并补一句该 300 秒超时未诊断原因（卡在输入等待还是计算中），这是"不可核验"级别的空洞记录。

2. **同段"首段真实文本……均不含 Talk_Common 占位符，因此不会触发目标泄漏"** 掩盖了一个未检验分支：这十名 NPC 的自慰结算同样可以触发 `v_orgasm_small`/`v_orgasm_normal` 二段行为（`Behavior_Data.csv:481-482`），而这两个 id 经 `game_config.py:1444` 的 part 键裁剪后**就在** `config_talk_common_cid_list_by_type` 里。文档只讨论了凯尔希的二段绝顶，对其余十人的二段路径只字未提。必须明写"十人的二段 V 绝顶路径未排除"。

3. **blocker 不够具体。** 它遗漏一个由已证事实直接推出的归约：`talk.py:251` 的判定是 `randint(1,100) <= rate*10`，CID 213 设为 100% 时该判定恒真（仅受权重 ≥100 特殊口上保护）。因此在 save 99 内，"是否存在正常可达的 B 触发"**完全等价于**"十名自慰 NPC 中是否有人能在重复 `[6001]` 等待内跨过 V 绝顶阈值"。事实 6 的"比例不能制造 B 触发"字面正确，但文档没有记录这条归约，导致 blocker 看起来比实际更开放。

4. **两帧证据规格不足，且自身有歧义。** "结算结束后重画的原交互目标标题"——在**基线**（有 bug）运行里，结算后标题恰恰应显示 B 而非原目标；按现在的措辞两帧只能描述修复后的运行，无法证明玩家可见问题。必须改为：
   - 同一连续受监督会话内**三个**基线帧：触发前标题=凯尔希、B 的通用文本帧（B 名字可辨）、结算后标题=B；
   - 加同 seed、同物理输入的候选侧运行，结算后标题=凯尔希；
   - 两帧之间无任何可合法改目标的玩家动作；触发角色或帧序不同则整对作废（技能的 freeze-randomness 规则）。
   仅"B 文本帧 + 原目标标题帧"两帧不能排除"目标被其他机制合法改变"的替代解释，也不构成技能要求的可比 A/B。

## 判定其余问题

- **事实/猜测/停止边界分离**：除上述第 1、2 点外基本合格；"可能使用""不保证""没有证据证明"等限定用得对。
- **tasks.md 3.2 保持未完成**：正确，不改。
- **spec 冲突**：无。"restored before control returns to the caller" 与"前提选择后立即恢复"相容。

## 具体、有限、可证伪的下一步（仍在 save 99 路线内）

只读解析 save 99：取出十名自慰 NPC 的 V 快感当前值与绝顶阈值，静态对照 `masturebate` 的 Behavior_Effect 结算项（1503/1513/1514/1521 等）与 V 绝顶二段行为的触发条件，计算是否有 NPC 能在 ≤12 次连续 `[6001]` 内跨阈值。产出二选一：命名候选 B 与所需等待次数（路线存在，转入 CID 213=100% + 固定 seed 的 Tk 采证），或给出数值证明阈值不可达（blocker 升级为有证据的结论，允许改写第 1 条那句"确认"）。成本是文件解析加算术，不是 300 秒运行时。
