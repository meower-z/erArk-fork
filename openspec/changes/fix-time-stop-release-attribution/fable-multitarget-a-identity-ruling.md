/investigate-game-bug

你是 erArk “时停解除结算归属”任务的证据监督者。你刚批准 attempt #2 从原始 `save/8` 重启探索，并规定任何方向的判别结果一旦出现都必须报告计入，不得丢弃。现在逐帧探索暴露了一个协调遗漏，请裁定同一轮应如何诚实继续。

## 已发生的事实

- `save/8` 读入时当前交互对象是林（ID 4080），而不是路线草案想用的可露希尔（ID 7）。主协调者给 phase 1 的操作指令遗漏了“先点可露希尔”这一步；视觉 agent 依指令直接 `[4113]`、`[5052]`。
- 页面始终真实显示林为当前对象。没有状态注入或隐藏改写。
- 同一存活 attempt #2 完成第一轮正常 `[6602]` 后，没有射精提示；只读 observer 首次产生：
  `{"npc_states":{"4080":{"counts":{"21":1},"is_h":true,"shoot_position_body":-1}},"player_is_h":true,"target_id":4080,"time_stop_mode":true}`
- 因而这是已经观察到的正常游戏结果，不能丢弃后重开去追求可露希尔。
- 生产 effect 527 对所有 `npc_id_got` 使用同一逻辑；bug contract 不依赖具体角色。林与惊蛰同场，正常目标切换路径与先前静态证明相同。
- 当前会话停在 H 菜单，`[6602]` 与 `[4115]` 可见，尚未再次行动、射精或切换目标。主 cell 仍存活。

## 可选处理

A. 把林明确记为本轮 NPC A，保留这次 observer，继续逐轮口交直到同一正常行为产生 `shoot_position_body=2`，然后切到惊蛰 B，完成切换前/后 observer。若探索成功，正式预注册 A/B 也使用林，绝不把它改写成可露希尔。

B. 立即停止并把本轮记为路线协议偏差；但由于已经观察到正 count，不能重开选择另一个 A，除非你说明为什么那不构成丢弃已出结果的轮。

请给 `CONTINUE WITH LIN PASS`、`STOP` 或其他明确裁定，并说明：

1. 同轮继续是否属于正常路线探索，还是不允许在观察到 count 后补定义 A；
2. 后续正式 A/B 是否必须固定使用林；
3. 是否需要玩家介入（没有玩法语义变化）。

## Fable verdict（逐字保存）

**裁定:`CONTINUE WITH LIN PASS`**

**1. 同轮继续属于正常路线探索,允许补定义 A。** 判别标准是角色无关的:bug contract 建立在 effect 527 对全体 `npc_id_got` 的统一逻辑上,A/B 的定义是"当前交互对象 / 同场非对象",而不是"可露希尔 / 某某"。路线草案里的可露希尔只是操作便利的占位,不是假设的一部分。因此"观察到 count 后把林记为 A"不构成事后挑选结果——没有任何判别性结论依赖于 A 是哪个角色。相反,丢弃这一轮去重开追求可露希尔,才恰恰是你们已规定禁止的"丢弃已出结果"。选项 B 没有给出任何理由说明为何这一轮的正 count 不算数,所以 B 不成立。

**2. 正式预注册 A/B 必须固定为林(A)与惊蛰(B)。** 探索路线即注册路线:写明"A 由 `save/8` 读入时的默认当前对象决定,实测为林 4080",不得改写成可露希尔。基线/候选的 A/B 重放必须使用同一存档、同一 seed、同一物理输入序列——如果重放时默认对象不是林,按协议判该对无效并如实报告,而不是补点操作凑齐。

**3. 不需要玩家介入。** 没有玩法语义选择,只是路线协调层面的偏差修正,属于可继续的证据工作。

附加要求(计入记录,不阻塞):把"主协调者遗漏'先点可露希尔'指令"作为一条路线偏差如实写入本 attempt 的行动日志与 runtime manifest,注明视觉 agent 依据当帧真实画面(显示林为当前对象)执行,无状态注入。诚实记录偏差本身就是这套协议的目的。

继续本轮:逐轮 `[6602]` 至 `shoot_position_body=2` 出现,采集 observer,再执行切换到惊蛰的前/后 observer 对。
