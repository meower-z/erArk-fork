# Fable ruling: attempt 4 viewport rehearsal route

Invocation: `claude -p --model claude-fable-5 --effort medium --tools "" --no-session-persistence`.

Prompt: [fable-formal-attempt4-rehearsal-route-prompt.md](fable-formal-attempt4-rehearsal-route-prompt.md).

Exact response follows.

```text
## 裁定：REVISE —— 路线差最后一步，一次补点即可

演练质量高（隔离、状态哈希、逐字节稳定判据、误判纠正流程都合格），但**路线的终点步骤未被演练**：`[4115]` 只被看见，没有被点击验证。一条止于终点前一步的路线演练，不能算完整重放过。逐题裁定如下。

**1. 判定：REVISE。** 已满足的部分：目标切换路线（两次 trough → 名单 → 点惊蛰 → 底部稳定 → sample 2 硬证明 target 306 且林逐字节不变）我认可为已演练成功。缺口只有一个：从回翻页点击 `[4115]` 能否真正进入 release 流程。

**2. 路线冻结：条件批准。** 你列的冻结路线在补证通过后即批准，外加一条必须补进的 anchor：**点击 `[4115]` 之后的预期画面判据**（release/结算页的可读锚文本）。现在的冻结路线终止于"点击一次 `[4115]`"，却没有定义点击成功长什么样——没有终点判据的路线无法判定"整侧无效"。"任一 anchor 不符即整侧作废、禁止补输入"的硬规则批准。另注意你的路线正确编码了切换前后 trough 行为不对称（切换前第二击才出名单，切换后第一击即名单、第二击到 grid），这来自输出历史长度差异，给定同 seed 同输入应当确定；不要"修正"它。

**3. 必须现在点，不能推迟到正式 A/B。** 理由：如果回翻页按钮的 callback 已失效（或点击导致视口重置到底部），在正式 A/B 中发现意味着报废一对 matched run，并制造临场补输入的压力——这正是 attempt 3 死掉的方式。在本 disposable 窗口中发现只花一次输入。担心"提前看到 baseline 结算结果污染判断"不成立：baseline 行为早已被代码阅读预测（CODE PASS），正式 A/B 的价值是新目录里的成对新鲜证据，不是执行者的无知。由此产生的一切结算画面**永久排除为正式证据**，只作诊断材料——这一条你自己已经提出，照此执行。

**4. 最小补演练步骤（就用存活的 r1-55 窗口，不必重开全程）：**
- 从当前画面测量可见 `[4115]` 的中心，点击**一次**；
- 零输入等待，取两次字节相同的稳定画面，`view_image` 复核；
- 若进入可读的 release/结算流程：记录终点 anchor 文本，路线完整，关窗、停服务、销毁本 runtime，随后新建正式目录预注册；
- 若画面不变或出现非预期内容：**不许尝试其它坐标或补操作**，截图、记 log、回来重新请裁定。

**5. 保留惊蛰可读证明循环。** 它不增加脆弱性——第一次 trough 点击（名单 + 惊蛰高亮 + 惊蛰状态同屏）本来就在通往 `[4115]` 的路径上，是免费的人类可读证据。一次上翻足够；不要求重复第二轮名单证明。

**6. Observer 需加一个字段后才够格。** 现在它只报林的经验快照。release 后 deferred counts 会被清算清零，届时区分"结算归了谁"的正面证据是**经验落在哪个角色身上**；若 baseline 错误归给惊蛰，现 observer 只能给出"林没变"的反面证据，缺 306 侧正证。正式前给 observer 加上惊蛰 306 的同组 selected experience 字段（与林对称），重新记 SHA 一并预注册。其余字段足够。正式路线中在点击 `[4115]` 结算稳定后必须取 sample 3。

**7. 不需要玩家介入。** 这是既有授权内的证据路线工作，不改玩法语义、无外发动作。补证完成且终点 anchor 确定后，直接进入正式 A/B 的新目录预注册，无需再次请示我路线本身；只有当补点失败或正式运行中 anchor 不符时才回来。
```
