/investigate-game-bug

你是 erArk “时停解除结算归属”任务的独立证据监督者。请以怀疑视角裁定一个刚由正常游戏 UI 生成的检查点，以及接下来的 Tk A/B 方案。不要因为代码级复现已经通过就默认玩家画面一定有说服力。

已确认事实：

- 精确 upstream 基线：`06fc59c1e71d092224375fc4a096b956aea2ad63`。
- 候选只改变 effect 527 的结算记录归属：正数计数改记到对应 NPC 的 `TargetChange`；零计数仍走原 `CharacterStatusChange`，不改变 `time_stop_release` 生命周期。
- 97 号检查点完全由真实 Tk UI 正常游戏路线生成，没有手改 pickle：读 98 号准备档；进入 `[5052] 无意识奸`；只做一次 `[6301] 阴道性交 → [01] 正常位`；`[6019] 拔出阴茎`；`[6005] 结束无意识奸`；回普通界面后用 `[008] 读写存档` 保存到空的 97 号档。没有点击 `[4114] 时间重新流动`。
- 主协调者重新只读打开 97 号档：`time_stop_mode=True`；博士 `target_character_id=7`、`behavior_id=share_blankly`；全存档只有一项非零延迟计数：可露希尔（ID 7）的部位 4（阴道）为 1；可露希尔仍是 `unconscious_h=3`。98 号准备档没有任何非零延迟计数。
- 97 号档 SHA256：头文件 `085f997182bedd71ec7737c86f1c2d753a423e68e93987f77ce744b895a522b5`；数据文件 `5a3cbd046242d8f4030b07bd1bc43d56fe02132e543908cd4eebd3d2413130f0`。
- 该存档将按字节复制到基线和候选；两侧用同一个 evidence-only launcher、`PYTHONHASHSEED=0`、Python/NumPy seed 5270714、2100x1100 geometry。launcher 只把 Xvfb 不支持的 `Wm.state/wm_state('zoomed')` 映射成 `normal`，并打印 fallback 计数；此前你已要求两侧计数必须都恰好为 1、首个输入前各截一张初始画面、任何其他 TclError 都使证据无效。
- 计划的正式玩家路线是：分别读取 97 号档，确认普通界面仍显示 `[4114] 时间重新流动`，在输入前截图，然后只输入 `4114` 并完成正常的结算继续步骤；截取包含博士与可露希尔结算分组的最终画面。
- 代码级真实加载器已经证明：基线在 effect 527 后 `root_exp_111=1, target_ids=[]`；候选为 `root_exp_111=0, target_ids=[7], target_exp_111=1`。正式 Tk 证据必须独立支持这个玩家可见归属差异，不能只引用代码输出。

请给明确裁定：

1. `CHECKPOINT PASS` 或 `CHECKPOINT FAIL`：这个正常 UI 检查点能否作为正式 A/B 的共同输入？若 FAIL，指出具体缺口。
2. `PLAN PASS` 或 `PLAN FAIL`：上面的最小 `[4114]` A/B 路线能否形成足以说服人类读者的 PR 证据？若 FAIL，给最小修正。
3. 列出正式截图和 manifest 的硬性通过条件，尤其说明最终画面必须出现哪些可读文字，才能证明“经验被记给了错误角色”而不只是布局变了。
4. 判断是否需要玩家现在介入。只有确实无法由仓库、正常 UI 或现有证据回答的游戏语义选择才标为玩家阻塞。

## Fable verdict (verbatim)

## 裁定

**1. CHECKPOINT PASS。** 97 号档由真实 UI 正常路线生成、双哈希已记录、只读复核确认了唯一非零延迟计数（ID 7 部位 4 = 1）且 98 号准备档为零，最小性和来源都成立。附带条件：复制到基线/候选两侧后必须各自重新计算 SHA256 并与记录值一致，写入 manifest；任一侧不一致即整对作废。

**2. PLAN PASS，但有两个必须先补的缺口，缺一则降为 FAIL：**

- **先确定经验 111 的玩家可见名称。** 现有事实只给了 `exp_111` 这个内部标识。在跑 A/B 之前，从 `data/csv` 的经验配置查出它在结算画面上的确切中文文本（如「XX经验 +1」），把这段文字写进 manifest 作为预期字符串。没有预先声明的预期文本，事后从截图里"找到一行像的"不构成证据。
- **逐页捕获结算输出，不只截"最终画面"。** 结算显示可能分多个等待页（`WaitDraw`/换行等待），任何一页翻过即丢。从输入 `4114` 起，每次按继续键之前截一帧，直到回到普通界面。证据对比必须取自两侧的对应页。若单帧截不到瞬时文本，用 ffmpeg 录 X display 抽帧。

其余计划（同档、同 seed、同 launcher、输入前初始帧、fallback 计数恰为 1）维持此前要求不变。

**3. 正式截图与 manifest 的硬性通过条件：**

截图（每张必须用 `view_image` 全分辨率复核可读）：
- 两侧输入前初始帧均可读地显示 `[4114] 时间重新流动`，且两帧内容等价（除无关时钟类差异外一致；不一致则整对作废）。
- **基线**最终结算帧：在**博士**的结算分组下出现经验 111 的预期文本行（含数值 +1），且**可露希尔**分组下没有该行。
- **候选**最终结算帧：同一文本行出现在**可露希尔**分组下，且博士分组下没有。
- 两帧中「博士」「可露希尔」的分组标识文字本身必须可读——归属差异要靠可读的角色名+可读的经验行共同证明，而不是行位置移动。
- 若某一侧结算画面完全不显示该经验行，如实记录为证据缺口并回来重新裁定，不得重试到出现为止。

manifest：baseline/candidate 源 ref（基线 `06fc59c1…`，候选 diff 哈希）、seed 5270714、`PYTHONHASHSEED=0`、launcher 路径与哈希、两侧存档复制后哈希、预期经验文本字符串、逐条带时间戳的输入日志（两侧物理动作序列必须相同）、每帧截图哈希与页序号、两侧 fallback 计数（均须恰为 1）、无其他 TclError 的声明。

**4. 不需要玩家现在介入。** 「时停解除后的延迟经验应记给被作用的 NPC 而非博士」是本候选的临时语义选择，属于我可授权继续的范畴，仓库与现有证据足以支撑完成本地候选。玩家的最终语义确认仍是**上游 PR 提交前**的必经门槛，届时再问，现在不阻塞。
