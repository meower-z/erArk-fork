/investigate-game-bug

你是 erArk“时停解除结算归属”正式 matched Tk A/B 的监督者。当前 attempt2 baseline 已严格通过鼠标点击读档、`[4113]`、`[5052]` 并停在 H 菜单。第一轮 `[6602]` 已产生正常结果，但暴露了预注册对等待页数的错误假设。会话正停在回到 H 菜单的画面，没有第二次 Return、没有第二轮口交、没有 signal。请以怀疑视角裁定当前轮应继续还是作废；不要因为重启很贵而放宽，也不要因为文档写了两次 Return 就忽略当前上游的真实画面。

## 预注册与实际帧

预注册对口交第 1~3 轮写了：点 `[6602]`，在 premise/value 页后 Return，再在 settlement 页后 Return，回到 H 菜单。这是从旧 ref 探索记录概括出的两页假设。

当前 upstream `abebf33b5` 的 attempt2 baseline 实际为：

1. `b2-08` 与上一阶段 H 菜单逐像素一致。
2. 鼠标点击当前可见 `[6602]`。
3. `b2-09-oral1-premise-value.png` 同一张完整帧中已同时可读：
   - 需要性与当前值；
   - `博士博士:` 与 `林 (时停):` 下的数值/经验变化；
   - `10分钟过去了`。
   本轮没有射精面板。
4. 之后恰好一次 keyboard Return，`b2-10-oral1-settlement.png` 已直接是 H 菜单，`[6602]` 与 `[4115]` 可见。
5. 视觉执行者发现页数不符后按 stopping rule 没有再按 Return。主协调者已亲自原尺寸查看 b2-09 与 b2-10，确认上述内容。

Candidate 尚未启动。候选唯一生产差异是 effect 527 中的 change-object 归属，不会在普通口交行为中执行。

## 需要裁定

请给一个明确结论：

- `CONTINUE / WAIT-PAGE RULE PASS`：保留当前已出现的第一轮结果，不重启；把第 1~3 轮规则更正为“每个实际出现的完整等待页都先截图，然后恰好 Return 一次；画面回到 H 菜单即停，不额外 Return”，并要求 candidate 对应轮画面边界与 Return 序列完全一致；
- `INVALIDATE`：作废当前 pair 并说明为什么必须丢弃已出现的第一轮结果；
- 或指定其他最小处理。

如果允许继续，请同时冻结第四轮的页面规则：不在未观测前硬编页数；射精面板必须首次出现于第四轮，选 `[射出]`；之后每个实际等待页都截图并 Return 一次，直到 H 菜单；candidate 必须出现相同页面边界、相同 Return 数与相同可读语义。任一侧多/少页、面板提前/缺失或语义不同即整对无效。

这只是当前上游的等待页边界修正，不改玩法语义、候选代码或外发状态。是否需要玩家介入？

## Fable verdict（逐字保存）

**裁定：`CONTINUE / WAIT-PAGE RULE PASS`。不作废，不需要玩家介入。**

## 理由

1. **偏差出在预注册的假设，不在执行。** 两页假设来自旧 ref 的概括，不是当前上游的观测事实。attempt2 的每一步都符合证据纪律：从当前帧选择动作、发现页数不符即停、主协调者已原尺寸复核 b2-09/b2-10。没有盲输入、没有多余 Return、没有存档污染——作废条件一个都不成立。
2. **candidate 尚未启动，无不对称风险。** 规则修正发生在两侧运行之间的唯一安全窗口：baseline 完整、candidate 零帧。改的是"如何数页"，不是"玩家做什么"——点击 `[6602]` 和逐页 Return 的物理输入序列不变。
3. **matched pair 的效力来自两侧可比性，不来自对错误预注册的服从。** 让 baseline 迁就一个已被真实画面证伪的页数假设，才是真正损害证据的做法。

## 生效的等待页规则（第 1~3 轮，追溯覆盖第一轮）

每个**实际出现**的完整等待页：先截图并 `view_image` 复核，然后恰好一次 Return；画面回到 H 菜单即停，不追加 Return。candidate 对应轮必须出现**相同页面边界、相同 Return 数、相同可读语义**（含 `10分钟过去了` 与双方数值变化在同一帧的结构）。任一侧多页/少页即整对无效。

## 第四轮规则（冻结，按你提议）

- 不在未观测前硬编页数。
- 射精面板必须**首次**出现于第四轮；在面板中选 `[射出]`。
- 此后每个实际等待页截图一次、Return 一次，直到 H 菜单。
- candidate 必须相同页面边界、相同 Return 数、相同可读语义。面板提前出现、缺失，或任一侧页数/语义不符 → 整对无效。

## 附带要求（一条）

在本 change 的记录与 runtime manifest 中**逐字保留**此次预注册偏差、修正后的规则文本、以及"修正发生在 candidate 启动前"这一时间事实，供事后审计。之后按既定路线启动 candidate。
