/investigate-game-bug

你继续监督 erArk “时停解除结算归属”正式 Tk A/B。Fable 上轮批准废弃误点会话并用键盘 `1 + Enter` 重建。新 baseline v2 已正确进入存档列表，但冻结路线遗漏了一个正常 UI 翻页动作。执行者在任何存档点击前再次停止，当前会话仍活着。请裁定最小修订以及是否可在当前会话继续。

事实：

- v2 新会话 ref/hash/launcher/slot98 均通过；首帧已截图。
- 键盘输入 ASCII `1`，截图确认输入框显示 `1`；Return 后截图确认进入正常存档列表，没有进入免责声明。
- 存档列表当前显示 `(0/9)`，可见 `[000]`–`[009]` 均为空；冻结路线下一步写成“点击 `[008] 98号档`”，所以当前可见 `[008]` 实际是空的 8 号档，不是 98 号档。
- 执行者识别差异后没有点击任何存档、没有翻页、没有读档、没有 gameplay、没有随机门或结算；现场保持在 0/9 存档列表。
- 旧探索的截图文件名是 `01-save-page9.png`，说明探索到选择 98 前确实处于第 9 页，但探索动作报告漏记了如何到达第 9 页。
- 存档面板提供可见 `[010] 上一页`；从第 0 页使用上一页按正常环绕逻辑应到第 9 页。到达后必须新截图确认标题为 `(9/9)` 且可见 `[008] 98号档`，不允许凭推测点击。

拟议最小修订：在冻结路线“进入存档列表”和“点击 `[008] 98号档`”之间加入：从当前帧重新选取 `[010] 上一页` 的按钮中心点击一次；截帧确认 `(9/9)` 与 98 号档可见；然后继续原路线。Baseline 和 candidate 都采用同一动作。把遗漏与修订写入 manifest 后再继续，当前 v2 会话不重启，因为尚未读档或观察 gameplay。

请裁定：

1. `LIVE CONTINUE PASS` 或 `RESTART REQUIRED`：写 manifest 后能否在当前 v2 存档列表现场继续？
2. `[010] 上一页` 从 0/9 环回 9/9 是否是合格的最小显式动作？是否必须先只读源码确认环绕逻辑，或当前/下一帧验证已经足够？
3. 这次遗漏在 manifest 需记录哪些事实？它是否算第二个废弃运行，还是同一有效 v2 运行的触发前路线补全？
4. 是否需要玩家介入？

## Fable verdict (verbatim)

**裁定**

**1. `LIVE CONTINUE PASS`。** 废弃标准是触发后污染：读档、gameplay、随机门消耗或结算观察。这些都没有发生——现场停在存档列表 `(0/9)`，会话身份四项校验（ref/hash/launcher/slot98）已通过。写入 manifest 后在当前 v2 会话继续，不重启。

**2. 合格，且不需要先读源码。** 冻结路线的验收标准是可见状态，不是翻页机制：从当前帧选取 `[010] 上一页` 中心点击一次，属于正常玩家交互；下一帧必须以新截图确认标题 `(9/9)` 且 `[008] 98号档` 可见后才允许继续。帧验证本身就是判据——环绕逻辑猜对猜错都无所谓，截图说了算。附加约束：如果点击后的帧不是 `(9/9)`（比如没有环绕），停止原计划，从新帧重新评估，此时才需要读源码或改用 `下一页` 逐页到达；不允许盲目连点。

**3. 记入 manifest 的事实，及分类：同一有效 v2 运行的触发前路线补全，不是第二个废弃运行。** 需记录：

- 冻结路线缺陷本身：探索报告漏记了进入第 9 页的翻页动作，证据是 `01-save-page9.png` 文件名与当前 `(0/9)` 帧的矛盾；
- 修订内容：在“进入存档列表”与“点击 `[008] 98号档`”之间插入“点击 `[010] 上一页` 一次 → 截图确认 `(9/9)` 且 98 号档可见”；
- baseline 与 candidate 使用完全相同的修订路线（这是 A/B 可比性的前提，candidate 侧不得走不同到达方式）;
- 继续依据：v2 会话在偏差点未读档、未 gameplay、未过随机门，故判定 LIVE CONTINUE。

**4. 不需要玩家介入。** 这是可逆的路线文档修订，未触及语义选择或对外动作。按修订路线继续执行。
