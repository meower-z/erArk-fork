```text
TITLE:
修复时停解除时干员的绝顶经验被错误记在博士名下

BODY:
## 问题

用时间停止对干员进行 H 时，她在时停中的绝顶会被延后累计，等到解除时停时一次性结算。这次结算正确地修改了该干员本人的状态，但产生的经验记录却被写进了博士的结算记录。因此解除时停后的结果页会多出一个博士名下的结算块，把本应属于该干员的「无意识绝顶经验」「饮精绝顶经验」等记在博士头上，该干员自己显示的对应经验相应偏少。数值总量没有丢失，只是显示归属错误。

## 修复

解除时停结算延后绝顶时，只要延后计数不为零，就为当前被解放的干员创建（或复用）她自己的 `TargetChange` 结算记录，并把这份记录传给原有的绝顶结算；计数全为零时行为保持原样。解除标记、计数清零、绝顶的数值公式和次数都没有改变，改变的只有这部分经验在结果页上的显示归属。

## 验证

在普通 Tk 模式中走同一条玩家路线：时停中对林进行 H，随后解除时停。

修复前，结果页出现博士名下的结算块，「无意识绝顶经验+1」和「饮精绝顶经验+1」被记在博士头上，林只有「无意识绝顶经验+2」：

[![修复前：博士名下出现本应属于林的绝顶经验]({{BEFORE_IMAGE_URL}})]({{BEFORE_IMAGE_URL}})

修复后，博士的结算块消失，林获得「无意识绝顶经验+3」和「饮精绝顶经验+1」，其余可见数值不变，经验总量没有丢失或重复：

[![修复后：绝顶经验全部记在林名下]({{AFTER_IMAGE_URL}})]({{AFTER_IMAGE_URL}})
```

修订内容：仅删除验证段第二句「修复前后直到结果页为止的所有画面完全一致，只有结果页不同。」，其余全部原样保留，文件未改动。（本次修订由 fable-5 直接执行——本会话主模型即 claude-fable-5，满足 skill 对 PR 文案须由 fable-5 撰写的要求。）
