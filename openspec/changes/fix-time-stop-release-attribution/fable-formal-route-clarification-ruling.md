/investigate-game-bug

你刚对 erArk“时停解除结算归属”给出 `FORMAL ROUTE PASS`。现在在任何正式 GUI 输入发生前，协议复核发现一个你的两条裁定在探索实际帧上不能同时满足的细节。请根据新补充的帧事实做最终选择，不要为了保持原裁定而默认两者等价。

## 冲突

你同时要求：

1. 正式短视口路线为“点滚动条上方 → 点惊蛰 → 点滚动条下方”；
2. pre-release gate 必须有“可读惊蛰选中帧”。

但探索的原尺寸帧已证明：

- `a2-35`：第一次点滚动条上方后，角色列表可读，林被选中，惊蛰可点。
- `a2-36`：点击惊蛰后，页面重绘回到较下方；立绘条改变，但当前帧没有可读的惊蛰名字或状态行。
- `a2-37`：只有再点一次滚动条上方，才能可读地看到 `[惊蛰]` 被选中及惊蛰状态行。
- `a2-38`：然后点滚动条下方，才回到 `[4115]` 可见帧。

因此，探索已实证能同时满足两个 gate 的最短已知视口序列是四步：

`点滚动条上方 → 点惊蛰 → 点滚动条上方取得选中证明 → 点滚动条下方回到 [4115]`。

## 请裁定

请选一：

- `FOUR-STEP ROUTE PASS`：冻结上述已实证的四步序列，baseline/candidate 逐步相同，每步都先从当前帧确认可点目标；
- `THREE-STEP ROUTE PASS`：保留三步序列，但明确放弃“可读惊蛰选中帧”，改由可读的点击前列表、点击动作日志、post-switch `target_id=306` 和立绘改变共同证明；
- `REVISE`：指定别的最小补证。

不得让 baseline/candidate 临场选择不同路线；如任一帧不符预期即停。

## 同时发现的 observer 盲点与拟修正

原 formal observer 只输出林，会隐藏其他 NPC 的未预注册非零 deferred state。现拟使用同一个只读 observer（SHA256 `e21ee71b9dbd679a6305ad40de6c5f3f25935ed6ae90b25cd4d58f07d7305053`），每次同时输出：

- 所有 NPC 的正 `time_stop_orgasm_count` 或非默认 `shoot_position_body`；
- 林 4080 的完整触发状态与经验 10/20/78/111/156；
- target、player/lin H、time-stop 和递增 sample index。

正式每侧恰好采样三次：

1. 切换前：`target_id=4080`，验证林 gate 和无其他 NPC trigger；
2. 切换后、`[4115]` 前：`target_id=306`，林对象必须与 sample 1 不变，且无其他 NPC trigger；
3. 点击 `[4115]` 并到达第一张数值结算画面后、再继续 Return 前：比较林五项永久经验 delta 两侧一致。

请一并裁定这个三样本/全 NPC 盲点修正是 `OBSERVER PASS` 还是需要其他最小修改。它不读取已离开作用域的 `change_data`，只证明路线状态与永久数值守恒；归属差异仍由正式 Tk 结算分组画面和真实 loader test 证明。

这两个都是证据协议修正，没有玩法语义变更或外发动作。请说明是否需要玩家介入。

## Fable verdict（逐字保存）

## 裁定

**FOUR-STEP ROUTE PASS。**

理由：帧事实（a2-35~38）已实证三步路线不存在可读的惊蛰选中帧，故我原来的两条裁定不能同时成立，必须修订其一。放弃的应该是路线长度，不是证据强度：

- 四步序列是唯一**已实证**同时满足两个 gate 的序列，无需再探索。
- 新增的第三步是滚动条点击，只改视口、不产生任何游戏输入，不影响结算状态或 RNG 消耗；baseline/candidate 逐步相同的前提下，它对 A/B 可比性是零成本的。
- THREE-STEP 会把"惊蛰被选中"从直接可读帧降级为多件间接证据的合取（日志 + `target_id` + 立绘变化）。人类审阅者看 PR 图像时，可读的 `[惊蛰]` 选中帧是一张图就能懂的证据，间接合取不是。既然直接证据只多花一次点击，没有理由降级。

冻结条件照你所述：四步序列逐字冻结，baseline/candidate 完全相同，每步点击前从当前帧确认可点目标可读，任一帧不符预期即停、判该对无效，不得临场改道。

## Observer 裁定

**OBSERVER PASS**，附三个执行条件：

1. **哈希核验**：两侧运行前各自核验 observer 文件 SHA256 等于 `e21ee71…7305053`，记入 runtime manifest；哈希不符即停。
2. **异常即停**：sample 1 或 2 若出现任何其他 NPC 的正 `time_stop_orgasm_count` 或非默认 `shoot_position_body`，该运行对作废并停下调查，不允许事后解释成"无关噪声"后继续。
3. **样本对齐**：两侧 sample index 序列必须完全一致（各恰好 1、2、3），缺样或多样即判该对无效。

这个修正方向正确：原 observer 只看林，等于假设了"只有林可能持有 deferred state"——而这正是待证命题的一部分。全 NPC 扫描把它变成被观测事实。三次采样点的选择（切换前 / 切换后-结算前 / 首张数值结算画面后）覆盖了归属转移的全部关键窗口，且不触碰已离开作用域的 `change_data`，与"归属差异由 Tk 结算分组画面 + loader test 证明"的分工不冲突。

## 是否需要玩家介入

**不需要。** 两项均为证据协议修正：无玩法语义选择、无外发动作、无生产代码变更。按 stopping rule，继续执行正式采集即可。语义最终确认仍保留在 upstream PR 提交前，那一步照旧需要玩家授权。
