/investigate-game-bug

你是 erArk “时停解除结算归属”任务正式 Tk 证据的独立监督者。正式 baseline 第一次启动在第一个输入就遇到 Tk 命中区域问题，执行者已按冻结规则停止。请裁定是否允许干净重建以及最小修正，不要因为已经投入很多时间而降低证据门槛。

已确认事实：

- 版本、baseline default.py、launcher、slot98 哈希全部匹配 manifest。
- 首个物理输入前已截图；launcher 打印 `EVIDENCE_WM_ZOOMED_FALLBACK_COUNT=1`，没有其他 TclError。
- 唯一输入原计划点击可见的 `[001] 神经重载`，截图坐标为 `(73,769)`；但游戏实际进入只属于 `[000] 初次唤醒` 的免责声明页。
- 源码只在 creator/new-game 路径调用免责声明，`[001] 神经重载` 应直接进入存档列表，所以这次点击实际上被 Tk 命中为 `[000]`。它不是隐藏的正常前置页。
- 执行者看到差异后没有再输入，没有点击免责声明，没有读档，没有进入 H，没有触发 `[4115]`。会话随后退出，allocator 已释放。无效运行的 initial/disclaimer/stdout/action log 均保留并有哈希。
- 探索路线此前成功使用过 `[001]`，但正式运行首次用坐标点击命中了错误按钮。这是触发前 UI 输入失败，不是 gameplay 随机差异。

拟议最小修正：

- 将正式路线的第 1 步从鼠标点击 `[001]` 改为键盘输入 ASCII `1` + `Enter`；存档页以后仍逐页截图并使用可验证的按钮/键盘输入。
- baseline 和 candidate 都使用同一修订后的物理输入序列；新会话从同一未改变的 slot98 开始，首个输入前重新截图。
- manifest 在任何有效 release 画面产生前加一段“废弃运行”记录，说明唯一点击被误判、没有进入 gameplay；随后冻结修订路线。废弃截图永不混入 A/B。
- 有效运行仍须在 `[4115]` 前得到 observer 原文、fallback=1、无其他 TclError，并逐页捕获结算。

请裁定：

1. `RESTART PASS` 或 `RESTART FAIL`：这次前触发误点是否允许按上述方式干净重开，而不构成挑轮次？
2. 若 PASS，键盘 `1 + Enter` 是否是合格的最小修正？是否需要把后续所有指令都改成键盘，还是保持探索中已验证的混合物理输入即可？
3. 无效运行在 manifest 中必须记录哪些事实，才能让人类审查者明白它未污染正式 A/B？
4. 是否需要玩家现在介入？证据输入工程困难本身不应被当作游戏语义选择。

## Fable verdict (verbatim)

**裁定:RESTART PASS**(附条件)。

**1. 允许干净重开的理由**

废弃这次运行不构成挑轮次,因为废弃决定的判据完全在 gameplay 之前:唯一输入命中错误按钮,进入了源码上只属于新游戏路径的免责声明页,没有读档、没有接近 `[4115]`、没有观察到任何结算或随机门结果。挑轮次的定义是"看到不想要的结果后重跑";这里不存在可被选择的结果。附加条件:

- 在新会话产生任何有效画面**之前**,先把废弃记录写入 manifest 并冻结修订路线(顺序不可反)。
- 必须验证并记录废弃会话退出后 slot98 的哈希与运行前一致——证明无效运行没有写存档。若哈希变了,slot98 需从 manifest 记录的原始来源重建,并把这一点也记入废弃段。

**2. 键盘 `1 + Enter` 是合格的最小修正,但要补一条根因防线**

修正合格的前提:新会话首帧截图能确认主菜单接受序号键盘输入,且输入后的截图确认落在存档列表页而非免责声明。这两个确认本身就是逐帧证据的一部分,不需要额外机制。

不需要把后续全部输入改成键盘。保持探索中已验证的混合物理输入即可,但注意根因:这次失败是**沿用了探索会话的坐标**,而 launcher 报告的 `WM_ZOOMED_FALLBACK` 意味着窗口几何可能与探索时不同,命中区域整体偏移。因此规则是——每次点击的坐标必须从**当前会话当前帧**重新选取,不得复用跨会话坐标;并且每次输入后的截图必须与预期状态匹配才允许下一次输入,任何不匹配立即按本次同样的冻结规则停止。这条规则本来就在技能规范里("Coordinates are acceptable only when selected from the current inspected frame"),正式运行不豁免。

**3. 废弃运行在 manifest 中必须记录的事实**

- 运行标识、时间、slot 分配记录;版本/baseline/launcher/slot98 哈希与主 manifest 匹配的声明。
- 保留工件清单及哈希:首输入前截图、免责声明截图、stdout、action log。
- 精确输入叙述:恰好一次物理输入,预期目标 `[001]`、坐标 `(73,769)`、实际命中 `[000]` 的免责声明页,以及判定依据(源码中免责声明只在 creator/new-game 路径调用)。
- 否定性事实:未点击免责声明、未读档、未进入 H、未触发 `[4115]`、未观察到任何结算或随机结果。
- slot98 运行后哈希 = 运行前哈希(见第 1 条)。
- 废弃判据的援引:触发前 UI 输入失败 ⇒ 废弃,且该判据在观察任何 gameplay 结果之前即成立。
- 声明废弃截图存放位置与"永不进入 A/B 对比"的隔离规则,以及紧随其后的修订路线冻结版本。

**4. 不需要玩家介入**

这是证据采集工程问题,不涉及任何游戏语义选择,也不涉及外发动作。按停止规则继续执行:写废弃记录 → 冻结修订路线 → 用同一 slot98、同一 seed、修订后的物理输入序列重启 baseline。证据门槛不变:`[4115]` 前 observer 原文、`fallback=1` 且无其他 TclError、逐页捕获结算。
