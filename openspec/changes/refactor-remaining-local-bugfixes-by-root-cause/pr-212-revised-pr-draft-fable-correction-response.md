我没有“刚才写的 PR #212 正文”——这是一个新会话，之前那份 TITLE/BODY 草稿不在我的上下文里，我无法凭空重建再做“最小定点修订”（那样会重写全文，违反你只改一处的要求）。

请把上一版完整的 TITLE/BODY（或它所在的文件路径/归档位置，比如 evidence 目录下的 draft 文件）贴给我。拿到原文后，我只改验证段最后一句的因果表述——把“修复前后都存在的正常副作用”改为准确含义（该副作用属于普通 state 17 路径的既有机制；baseline 因负苦痛提前改道 state 23 并 return，未进入 `extra_feel_settle`；修复后负变化重新落回该路径，故候选画面显示它，证明普通结算被保留而非转换残留），其余一字不动，并按要求输出完整 markers。
