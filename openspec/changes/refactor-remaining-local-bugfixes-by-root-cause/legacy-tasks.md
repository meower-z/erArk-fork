## 0. Audit And Coordination Baseline

- [x] 0.1 Identify active PR/worktree ownership and exclude commission display, cross-platform saves, group target restoration, hidden witness deduplication, scene-wide premise loops, and time-stop 5052
- [x] 0.2 Run parallel read-only audits of group/H, hypnosis/state, and input/UI mods against current upstream source and tests
- [x] 0.3 Add the repo-local `investigate-game-bug` skill with clue, reproduction, root-cause, preventive-refactor, and causal-radius gates
- [x] 0.4 Record focused test baselines and distinguish passing component tests from the timed-out group-AI BDD and unrun platform/gameplay checks

## 0A. Assumed-Upstream Dev Baseline

- [x] 0A.1 Preserve reviewed WIP on a stable local `main` branch and create a separate local `dev` branch without pushing either branch
- [x] 0A.2 Apply the exact production commits from upstream PRs 204, 205, 206, 207, and 210, recording each commit identity
- [x] 0A.3 Disable or retire the commission, cross-platform-save, and live group-target mod responsibilities; remove only the `place_all_not_h` and hidden-witness responsibilities from the mixed participant-admission mod
- [x] 0A.4 Make the remaining enhanced `place_all_not_tired` mod logic delegate to the corrected core predicate instead of copying its full-scene traversal rule
- [x] 0A.5 Verify every overlaid core path with the duplicate wrapper disabled, including a non-type-1 group-AI path that reaches target-sensitive status selection
- [x] 0A.6 Record the per-PR rollback/rebase boundary and keep screenshot/test evidence worktrees out of this code-cleanup step

## 0B. Single-Main Worktree Policy

- [x] 0B.1 Fast-forward the complete `dev` state onto local `main`, switch to `main`, and delete `dev`
- [x] 0B.2 Remove the five independent PR clones and their ten dependent evidence worktrees after confirming the PR commits are preserved
- [x] 0B.3 Update `investigate-game-bug` so every PR candidate uses a fresh linked worktree from `upstream/master`, never a new clone
- [x] 0B.4 Create separate linked worktrees and `codex/` branches for the movement-talk, type-1 group-AI ordering, and Tk queue-rendering candidates
- [x] 0B.5 Create separate linked worktrees and `codex/` branches for the invitation-lifecycle and ordinary edge-failure candidates after their pre-implementation design gates pass
- [x] 0B.6 Create a linked worktree and `codex/` branch for H-entry movement-plan cancellation after two independent boundary reviews agree
- [x] 0B.7 Create linked worktrees and `codex/` branches for tired group-exit commit ownership and signed pain routing after their independent design gates pass
- [x] 0B.8 Create a linked worktree from `upstream/master` and a `codex/` branch for the authoritative two-caller hypnosis-runtime cleanup slice after its independent boundary review passes
- [x] 0B.9 Create a linked worktree from `upstream/master` and a `codex/` branch for the approved discovery-settlement ownership candidate
- [x] 0B.10 Create a linked worktree from `upstream/master` and a `codex/` branch for the approved current-group-membership candidate
- [x] 0B.11 Create a linked worktree from `upstream/master` and a `codex/` branch for the approved atomic declared-new-registration candidate

## 1. Failing-First Reproductions And Semantic Gates

- [x] 1.1 Add a red regression using real `data/talk/system/move.csv` templates and a production-reachable same-scene NPC path that proves multi-placeholder movement text keeps the NPC actor and scene-name formatting
- [ ] 1.2 Decide and record whether a long player window produces one continuous group masturbation action or repeated shorter actions, then build the corresponding red action-window matrix
- [ ] 1.3 Decide repeated identical orgasm-effect multiplicity and per-exit pending-edge visibility/discard rules before modifying batch or finalizer behavior
- [x] 1.4 Build paired near-real discovery reproductions for the NPC state-machine caller and direct hidden-sex caller, recording exact settlement counts and nested updates
- [ ] 1.5 Build prompt-generation red tests for stale, missing, duplicate, cross-channel, reconnect, and concurrent Tk/Web responses before changing the input protocol
- [x] 1.6a Trace Tk font construction, mod timing, development/package paths, and renderer lifetime; confirm that named-font creation success is not file-load proof
- [x] 1.6b Pass an independent boundary critique, define the exact Windows red/green matrix, and stop production edits because Linux fake-GDI evidence cannot confirm the Windows player defect
- [ ] 1.6c Obtain clean-Windows development and packaged-build evidence for private Sarasa registration, Unicode paths, idempotence, missing/corrupt files, renderer isolation, and `Font.actual`

## 2. Small Independent Direct Fixes

- [x] 2.0a Record and independently critique the movement-talk violated rule, owner, alternative boundaries, sibling/inverse coverage, and non-goals before accepting its existing implementation shape
- [ ] 2.0b Resolve the type-1 gameplay semantic contract before selecting any production boundary; keep the candidate stopped meanwhile
- [x] 2.0c Record and independently critique the Tk scroll transaction boundary, including non-scrolling layout operations and exception behavior, before resuming production edits

- [x] 2.1a Reproduce movement-talk actor corruption with real `move.csv` templates and inverse common-token cases on untouched upstream
- [x] 2.1b Fix common-talk token classification at the formatter owner and verify sibling multi-placeholder templates
- [x] 2.1c Rewrite the movement-talk Chinese PR draft from scratch for upstream review, excluding local-only evidence, investigation scaffolding, redundant implementation narration, child-operator names, nicknames such as `小兔子`, and local/custom character names
- [x] 2.1d Have an independent agent verify that the final movement-talk PR title/body is self-contained, contains no redundant information, and remains understandable at every prefix using only the submitted diff and PR-visible evidence
- [x] 2.2a Reproduce the type-1 template-member early return on untouched upstream without relying on target-restoration behavior
- [ ] 2.2b Move type-1 policy evaluation before template membership with behavior-only tests and unchanged non-type-1 cases
- [x] 2.2c Produce a repeatable interaction flow, appropriate evidence, and a local Chinese PR draft for type-1 ordering
- [ ] 2.2d Pass a fresh-context scope and human-reviewability review for type-1 ordering, revising actionable feedback
- [x] 2.2e Stop the type-1 candidate after fresh review confirms a semantic blocker: both player-facing settings text and premise documentation limit type 1 to NPCs outside the template
- [x] 2.3a Trace and independently critique group-sex invitation cancellation separately from new-member admission, including pending second-behavior ownership
- [x] 2.3f Reject both destination-shape and persisted-route-owner invitation designs after production tracing shows upstream WAIT already cancels travel; independently redesign around invitation-local pending-state ownership
- [x] 2.3b Introduce invitation-local start/clear/cancel operations, keep upstream WAIT cancellation, clear only this invitation's pending acceptance text, and roll back when a route cannot be created
- [x] 2.3c Produce production-path red/green evidence and a local Chinese PR draft for invitation lifecycle cancellation
- [x] 2.3d Have an independent agent review invitation lifecycle scope and verify that the final PR title/body is self-contained, contains no redundant information, and remains understandable at every prefix using only upstream-visible material
- [x] 2.3e Stop shared fatigue/admission and arrival recheck work pending authoritative semantics for which rules re-evaluate on arrival and how rejection transitions clean up
- [x] 2.3g Confirm discovery settlement ownership across both production callers and reject per-callback call patches, global suppression, and an over-broad follow-up plan object
- [x] 2.3h Pass a second design gate for the smallest typed discovery-dispatch result and explicit no-retry exception contract before production edits
- [x] 2.3i Implement the approved discovery settlement owner, paired red/green caller matrix, evidence, and Chinese PR draft in a separate upstream-based worktree
- [x] 2.3j Pass independent code-scope and upstream-context PR-text review for discovery settlement ownership
- [x] 2.3k Freeze the stronger fixed-seed repeated-discovery evidence and trace the stale target hidden flag through production small-orgasm second effect 411, rejecting early MOVE advancement as a surface patch
- [x] 2.3l Review and reject the one-file hidden-session extension after clarifying that a different eligible witness may discover the same encounter
- [x] 2.3m Preserve the rejected `hidden_sex_panel.py` extension, tests, and Nine-versus-no-second-prompt package as local investigation history only
- [x] 2.3n Rebuild the current-upstream/four-file-candidate A/B with Python `random`, NumPy RNG, and `PYTHONHASHSEED` fixed identically, proving only the missing or duplicated discoverer reaction before replacement Fable/artifact review
- [x] 2.3o Separate the already-merged same-witness defect from settlement ownership and build the corrected fixed-seed package: Closure repeats before PR #206, while after the fix Closure is excluded and Nine remains an allowed next witness
- [x] 2.4a Reproduce and benchmark redundant Tk `see_end()` calls on untouched upstream without including wait/input behavior
- [x] 2.4b Introduce a bounded Tk consumer render transaction that coalesces each consecutive auto-scroll segment while preserving FIFO, viewport barriers, direct calls, and exception behavior
- [x] 2.4c Produce a repeatable interaction flow, production-consumer benchmark evidence, and a local Chinese PR draft for Tk queue rendering
- [x] 2.4d Have an independent agent re-review Tk queue scope and verify that the final PR title/body is self-contained, contains no redundant information, and remains understandable at every prefix using only the submitted diff and PR-visible evidence
- [ ] 2.5 Prepare private font registration inside Tk bootstrap after Windows proof, verify the actual family, and remove `local_fontfix` only after development and packaged checks pass

## 3. State Transition Owners

- [x] 3.1a Trace and independently critique hypnosis runtime cleanup across effect 489, explicit cancel, single/group sanity exhaustion, air-hypnosis scope, paired active-H fields, and non-hypnosis unconscious states
- [x] 3.1b Introduce one idempotent target hypnosis-runtime cleanup operation that preserves codes 1-3 and persistent degree/ovulation state, then migrate only callers whose target-selection and side effects are authoritative
- [x] 3.1c Stop the remaining hypnosis migration pending decisions on group sanity target scope, explicit-cancel target settlement, and air-hypnosis door provenance; do not infer these from current flags
- [x] 3.1d Produce upstream red/green state evidence and a self-contained Chinese PR draft for the two-caller hypnosis-runtime slice, explicitly retaining the unresolved callers and peripheral side effects
- [x] 3.1e Pass independent code-scope and upstream-context PR-text review for the two-caller hypnosis-runtime slice
- [x] 3.2 Stop the proposed broad hypnosis talk-gate bypass because no production reproduction or per-mode reaction contract supports treating modes 4-7 alike
- [x] 3.3a Reproduce and trace signed pain-delta routing across common positive/negative settlement and direct effects 270/283/296/408 under the accepted raw-flag, sleep, cap, and accounting contract
- [x] 3.3b Pass a fresh-context design critique that fixes the common double repeat adjustment, requested-value records, direct formulas, and per-entry death semantics before production edits
- [x] 3.3c Add a narrow positive-pain router and migrate common settlement plus direct effects 270/283/296/408 without copying the general state-settlement formula
- [x] 3.3d Produce exact red/green accounting evidence and a self-contained Chinese PR draft for signed pain routing
- [x] 3.3e Pass independent code-scope and upstream-context PR-text review for signed pain routing
- [x] 3.3f Rebase the reviewed two-file candidate onto current `upstream/master`, publish the approved commit-pinned Tk images, and open upstream PR [#212](https://github.com/Godofcong-1/erArk/pull/212) from `meower-z/erArk-fork`
- [x] 3.4a Trace and independently critique movement history, route commitment, player-loop interruption, and every H-entry writer before production edits
- [x] 3.4b Introduce idempotent movement-plan cancellation that clears only `move_final_target`, invoke it from canonical H entry and proven direct writers, and make the active player loop observe revocation
- [x] 3.4c Produce near-real red/green evidence and a local Chinese PR draft for H-entry movement-plan cancellation
- [x] 3.4d Have an independent agent review movement-plan scope and verify that the final PR title/body is self-contained, contains no redundant information, and remains understandable at every prefix using only upstream-visible material
- [ ] 3.5 Verify each state owner through real entry, cleanup, inverse, exception, and independent-mod paths before disabling the replaced responsibility

## 4. NPC Action Window And Transition Commit

- [x] 4.1a Trace the implicit player-action window and reproduce repeated group-masturbation intent consumption within one catch-up window
- [x] 4.1b Stop action-window implementation until gameplay defines whether a 60-minute player action produces one 60-minute automatic action or six 10-minute actions, and how unavailable targets retry
- [x] 4.2a Reproduce and independently critique tired group-exit configuration being overwritten by the later H guard without any effect-chain commit
- [x] 4.2e Reject the first tired-exit candidate after tracing the real zero-participant group-end effects and nested scheduler epoch; do not serially commit group end and NPC exit
- [x] 4.2f Trace all low-level tired callers, live-membership sources, multi-leaver reduction, nested time advancement, and scheduler epoch ownership before redesign
- [x] 4.2g Stop the full tired-transition implementation pending decisions on zero-survivor desire, whether 375 ends group mode, simultaneous follow plus H priority, and pre-window fatigue timing
- [ ] 4.2b Return a narrow tired-exit decision and let the scheduler commit that exact transition once while skipping only later pre-behavior guards
- [ ] 4.2c Produce production-path red/green evidence and a self-contained Chinese PR draft for tired group-exit commit ownership
- [ ] 4.2d Pass independent code-scope and upstream-context PR-text review for tired group-exit commit ownership
- [ ] 4.3 Migrate group masturbation generation and successful consumption off `over_behavior_character` identity and process-global active sets
- [ ] 4.4 Migrate tired group-exit behavior off the edge component's manual re-settlement wrapper
- [ ] 4.5 Verify retries, exceptions, nested/different NPCs, `1/5/10/60` minute windows, group exit, and next-window regeneration with exact effect and talk counts

## 4A. Current Group Membership

- [x] 4A.1 Pass an independent design gate for a group-sex-owned current-participant query that excludes non-H bystanders, pending invitees, and off-scene stale state without using templates as membership
- [x] 4A.2 Implement current group membership and migrate tired survivor classification, including a caller-membership guard, in a separate upstream-based worktree
- [x] 4A.3 Produce upstream red/green evidence and a self-contained Chinese PR draft for current group membership
- [x] 4A.4 Pass independent code-scope and upstream-context PR-text review for current group membership

## 5. Orgasm Transaction And Exit Finalization

- [x] 5.1a Trace and independently critique ordinary failed-edge delta consumption, later-part processing, release state, and ledger lifecycle before production edits
- [x] 5.1b Refactor ordinary edge failure into one `orgasm_settle` state-machine call that consumes each delta once, releases old/current/later work once, clears the ledger, and preserves release state 2
- [x] 5.1c Produce exact-value red/green evidence and a local Chinese PR draft without presentation or action-window policy
- [x] 5.1d Have an independent agent review ordinary edge-failure scope and verify that the final PR title/body is self-contained, contains no redundant information, and remains understandable at every prefix using only upstream-visible material
- [ ] 5.2 Give orgasm detection an explicit consumable delta/result so a completed release cannot be detected again from the same cumulative change object
- [x] 5.2a Stop effect-526 repeated-detection work until gameplay semantics confirm whether release-generated pleasure may chain into another climax in the same closure
- [x] 5.2b Reproduce cumulative level-10 delta re-consumption, genuine release-generated pleasure, 0/1 versus counted multiplicity, and both retry-duplicate and destructive-cleanup failure modes
- [x] 5.2c Stop transaction and exit-finalizer implementation until release chaining, identical-effect multiplicity, per-exit visibility/discard, window-exit handling, and failure policy are explicit
- [ ] 5.3 Introduce a counted atomic orgasm transaction that separates calculation/commit from presentation and preserves retryable state on failure
- [ ] 5.4 Introduce `finalize_pending_edges` in the orgasm domain and call it before each proven H/group reset or participant-removal path
- [ ] 5.5 Move compact display, remote visibility, human-power aggregation, and window-end edge policy into separate consumers or follow-on changes
- [ ] 5.6 Verify normal/group/time-stop settlement, all exit reasons, same/remote scenes, exception retry, exact queue end state, and Tk/Web presentation before removing batch/edge mod responsibilities

## 6. Cross-Renderer Prompt Protocol

- [x] 6.0a Reproduce stale, duplicate, missing, cross-kind, concurrent, and cross-client responses against the real Tk queue and Web endpoints, confirming the absence of prompt identity
- [x] 6.0b Stop implementation pending explicit cancellation, reconnect, multi-tab, concurrent-instruction, and dialog-skip ownership semantics; timing delays and queue drains are not accepted substitutes
- [ ] 6.1 Define the core prompt record and one-shot state machine for command, wait, dialog, and string input kinds
- [ ] 6.2 Make Tk events and Web requests carry the current prompt ID and reject stale, duplicate, missing, or cross-kind responses without draining unrelated input
- [ ] 6.3 Move final instruction premise and current-rendered-command authorization into the instruction core and serialize execution on the game thread
- [ ] 6.4 Give skip behavior an explicit prompt/operation owner and remove entry-flag inference, dialog snapshots, polling, and global recording-flag toggles
- [ ] 6.5 Run reconnect, cancellation, concurrent-request, fast-input, double-click, Enter, and next-panel tests in Tk and Web
- [ ] 6.6 Remove the `local_performance` wait patch and disable/delete `local_settlement_input_fix` only after the new protocol passes before/after gameplay checks

## 7. Temporary Mod Loading And Cleanup

- [x] 7.1 Inventory every enabled mod's declared, import-time, external, and delayed runtime mutations and stop adding new self-installing wrappers
- [x] 7.1a Reproduce undeclared mutation leaks, data-merge leaks, wrapper-chain bypass, and false-success `type: new` registration through the real loader
- [x] 7.1b Pass a fresh-context design critique that separates atomic declared-new registration from any future staged patch transaction
- [x] 7.1c Make one `type: new` plus `register_to` operation publish its internal and module registrations together or restore both prior values and fail the mod load
- [x] 7.1d Produce untouched-upstream red/green loader evidence and a self-contained Chinese PR draft for atomic declared-new registration
- [x] 7.1e Pass independent code-scope and upstream-context PR-text review for atomic declared-new registration
- [ ] 7.2 If a retained runtime mod must evolve, propose a separate transaction for loader-registered staged operations, with validation, reverse-order rollback, and explicit ordered-wrapper composition tests; do not claim arbitrary `exec` is transactional
- [ ] 7.3 Update `group_sex_extension` to consume admitted-participant and orgasm-owner interfaces without swallowing broad exceptions or directly mutating owned state
- [ ] 7.4 Remove replaced functions from manifests and installers one responsibility at a time; update default config and migration docs only after independent equivalence tests pass
- [ ] 7.5 Keep every future upstream change in a fresh linked worktree from `upstream/master`, never a new clone, and request separate authorization before push, evidence publication, or PR changes

## 8. Final Verification And Review

- [ ] 8.1 Run each focused red/green regression, real-loader isolation suite, relevant near-real BDD, and the full maintained-mod composition in separate processes where global fixtures require it
- [ ] 8.2 Run Tk and Web visual behavior checks for every input, panel, wait, or visible settlement change and Windows checks for font work
- [ ] 8.3 Re-open each changed source artifact, inspect the actual diff against its owner-level contract, and obtain a fresh reviewer pass
- [ ] 8.4 Confirm active PR workstreams remain untouched, record any skipped or timed-out verification, and synchronize main specs only after implemented behavior is proven
- [x] 8.5 Run a second independent root-cause audit across every direct candidate after implementation review, classifying each as owner-level, insufficiently proven, surface-only, or semantically blocked before it can be called ready

## 8A. PR Artifact Visibility Audit

Evidence rule corrected by user (2026-07-12): each behavior-changing PR needs one main, easy-to-understand Tk before/after case, not an image set for every sibling. Prepare a reproduction save plus written player steps, then have a local visual subagent operate the real Tk window. The established route is local Xvfb/Tk, ImageMagick capture, `view_image` pixel inspection, and one visually chosen local `xdotool` action before the next capture. Do not use blind coordinate or command batches, direct gameplay-state mutation, VNC/noVNC, or any network relay. A fixed seed may stabilize chance, but the visual subagent still chooses actions from inspected frames.

Evidence priority update (2026-07-12): defer Tk queue performance capture because a normal player workload and responsive-during-processing evidence are unusually difficult to establish. Continue one representative Tk case per other candidate; Web evidence is not required.

Current batch snapshot (2026-07-13): the previous all-candidate HTML was withdrawn. Full PR material may appear only for candidates with inspected visual-agent Tk evidence, a `fable-5` medium-effort draft, and a passing fresh-context prefix audit. Blocked, rejected, and withdrawn work may appear only in a separate status summary and must not expose stale drafts as review-ready.

- Hypnosis target cleanup was narrowed after the user confirmed that `苦痛快感化` persists across sleep exit and generic `npc_active_h` is not hypnosis-owned. The replacement sleep-exit Tk evidence, `fable-5` draft, and fresh artifact review passed; after separate publication authorization, commit `e1a9378b1` was opened ready for review as upstream PR [#213](https://github.com/Godofcong-1/erArk/pull/213).
- Invitation lifecycle was narrowed to `edcc951b5`, one file and one cancellation-visibility rule. Its code review passed, but the only proposed player route depends on an exact 24-minute arrival boundary and the visible impact is minor. After discussion, the user decided it is not worth fixing upstream; retain the diagnosis and candidate locally without further plan review, capture, PR prose, push, or publication.
- Ordinary edge failure was redesigned as change `judge-orgasm-edge-once-per-settlement`. The new one-file collect/decide/apply candidate and focused submitted test passed implementation review in `/home/ubuntu/games/erArk-pr-edge-shared-settlement`; Tk evidence remains blocked because untouched upstream raises `KeyError: 0` while loading `save99` before the target scene. Keep PR prose blocked until a valid normal save yields inspected A/B images.
- Movement talk is evidence-plan blocked. The fair `slot 10`, `seed=20260712` run proved recall only requests following: Kal'tsit's breakfast need wins target selection and routes her to the dining hall, while the Doctor's private room later rejects entry. The earlier seven-edge estimate proved pathfinding only, not scheduler reachability; invalid frames were removed and no PR evidence image exists.
- Movement-talk evidence runtimes may apply cross-platform-save PR commit `2dd4e9d6b` identically on baseline and candidate so untouched Windows checkpoints load on Linux. That shared overlay is testing infrastructure only and must not enter the movement-talk branch diff, PR prose, or verification claims.

- H-entry movement cancellation is withdrawn. Production tracing found no normal route that enters H while a multi-segment movement plan remains unfinished; the candidate tests synthesize that state, and no truthful Tk case exists.
- Signed-pain routing is complete and open for upstream review as PR [#212](https://github.com/Godofcong-1/erArk/pull/212) at commit `21261e951`. The final candidate changes only `Script/Settle/common_default.py` and `Script/Settle/Second_effect.py`; 27 focused local tests passed after rebasing. The accepted real Tk A/B and fresh artifact review cover the same byte-identical proposed production diff captured before that final parent-only rebase, so no post-rebase Tk replay is claimed.
- PR #212's Windows workflow succeeds through PyInstaller, game/config/cache builds, all package creation, and artifact upload. Its final release-creation step fails with `Resource not accessible by integration` because the fork PR integration cannot publish an upstream release; do not report that check as a production build failure.
- The type-1 ordering candidate is rejected: the on-screen contract applies the setting only to participants outside the template. A separate marker-3 lifecycle trace confirmed a short-window overwrite mechanism, but every proposed fix would choose unresolved automatic-action frequency, duration, or replanning policy; that work is plan-stage blocked.
- Discovery settlement is reopened after the user clarified that a different eligible witness discovering the same encounter is allowed. The pre-#206 same-Closure repetition remains frozen historical evidence, and PR #206 owns that rule on current upstream. The corrected four-file candidate ends at `5d360f71e`; `e281ebdda`, its hidden-session teardown, the Nine-versus-no-second-prompt GIF, and the associated Fable/artifact PASS are withdrawn. The replacement current-upstream A/B fixes Python `random`, NumPy RNG, and `PYTHONHASHSEED` identically and proves the discoverer's missing reaction rather than later witness selection; its replacement Fable draft and artifact review passed for local review, while publication remains separately gated.
- Current-group-membership candidate `cc54e1da8` is withdrawn. Excluding bystanders changes only the unresolved zero/one-survivor transitions, so a membership-only behavior PR cannot truthfully avoid scheduler-epoch semantics.
- Atomic new registration candidate `8d5a582e3` passed its declaration-level code review. The root cause and narrow fix are known, but no direct, decisive Tk evidence exists; the prepared fixture demonstrates only an indirect downstream effect. The user decided the defect is not currently worth filing upstream, so retain the diagnosis and candidate as local reference without further capture or PR work.
- Tired group-exit candidate `4d3515972` is withdrawn. A re-entrant per-character fatigue guard cannot own scene-wide reduction and nested scheduler transitions; replacement design remains blocked on zero-survivor desire, one-survivor conversion, simultaneous follow-plus-H priority, and fatigue-window timing.
- Tk queue performance evidence remains explicitly deferred by the user and is not exempt from the image rule.

- [x] 8A.1 Update both skills for `fable-5` medium-effort PR writing, an erArk-player reader baseline, one representative Tk case, and save-plus-written-route local visual-subagent evidence with screenshot-led single-step interaction
- [x] 8A.2 Inventory the 12 PR drafts newly created by this workstream, their PR-facing evidence, and exact proposed diffs; exclude user-approved/open upstream PRs, superseded copies, and handoff notes
- [ ] 8A.3a Complete the detailed movement-talk exploration, deterministic A/B capture, Fable writing, and fresh-context review workflow in section 8B
- [x] 8A.3b Audit the type-1 ordering candidate, reject its unsupported gameplay contract, and keep the separate marker-lifecycle investigation blocked until automatic-action cadence is decided
- [ ] 8A.3c Deferred by user: return to Tk queue performance capture after the other behavior-fix evidence; keep non-performance behavior evidence blocked, not waived
- [x] 8A.3d Withdraw narrow invitation cancellation visibility from upstream PR preparation: the exact-boundary issue is too minor to justify further plan review, Tk capture, or PR artifacts unless the user explicitly reopens it
- [ ] 8A.3e Complete `judge-orgasm-edge-once-per-settlement`: preserve its reviewed one-file implementation, replace the broken save99 evidence route with a normal loadable save, then obtain visual A/B evidence before Fable writing and fresh-context review
- [x] 8A.3f Withdraw H-movement cancellation after production tracing finds no normal multi-segment-route H-entry case; do not present its synthetic tests or stale draft as a PR
- [x] 8A.3g Complete the exact signed-pain Tk case, inspect the matched A/B images, pass `fable-5` medium drafting and fresh-context artifact review, publish the approved images, and open upstream PR [#212](https://github.com/Godofcong-1/erArk/pull/212)
- [x] 8A.3h Complete `prepare-hypnosis-target-runtime-pr-artifacts`: narrow the candidate to hypnosis-owned cleanup, capture replacement sleep-exit evidence, rewrite title/body/evidence with `fable-5` at medium effort, obtain a fresh-context `PASS`, and record the separately authorized upstream PR #213 publication
- [x] 8A.3i Complete the corrected four-file discovery-settlement candidate, preserve the old same-witness evidence as history, capture a deterministic current-upstream A/B of the discoverer reaction, then replace the withdrawn Fable draft and obtain fresh artifact review
- [ ] 8A.3j Decide the zero/one-survivor and scheduler-epoch semantics before replacing the withdrawn current-group-membership candidate; require images for any replacement
- [x] 8A.3k Withdraw atomic new-registration from upstream PR preparation: retain the known root cause and reviewed narrow fix locally, but do not spend further effort on indirect Tk evidence or PR artifacts unless the user explicitly reopens it
- [x] 8A.3l Withdraw the rejected tired-transition candidate pending gameplay decisions and require images for any replacement candidate

## 8B. Movement Talk Actor Context Evidence And PR Completion

- [x] 8B.1 Record the exact movement-talk baseline/head refs, Python/Tk environment, enabled mods, candidate worktree state, and PR-visible diff; classify untracked tests and temporary artifacts as local-only unless deliberately added to the proposed diff
- [x] 8B.2 Remove the disproved `slot 10` Doctor-room route and invalid frames from the active evidence plan and stale PR draft without deleting the private blocker record
- [x] 8B.3 Define a bounded manual exploration log for copied saves that records each player action, game time, player/NPC scenes, visible behavior or talk, and the scheduler priority or admission rule that ended the trial
- [x] 8B.4 Establish a local evidence-only launcher that fixes `PYTHONHASHSEED` before interpreter start and a hook that seeds Python `random` and NumPy after load but before the first recorded action, without operating the UI or changing gameplay state
- [ ] 8B.5 Explore short normal Tk routes one trial at a time, using observed NPC behavior to vary the public destination, NPC, time window, or player action until the scheduler produces an affected arrival template; do not edit positions, needs, targets, routes, talk weights, or completion state
- [ ] 8B.6 Repeat the selected route twice on untouched baseline from the same copied checkpoint and prove identical player/NPC scenes, scheduler choices, arrival time, and affected movement template under the recorded random environment
- [ ] 8B.7 Preserve pristine baseline/candidate checkpoint copies, apply cross-platform-save PR `2dd4e9d6b` identically as an evidence-only runtime overlay when needed, and write the exact player-visible route from load through arrival, including the fixed interpreter and random settings
- [ ] 8B.8 Give the checkpoint and written route to a local visual subagent, which SHALL capture and inspect each real baseline/candidate Tk frame before choosing one local `xdotool` action; prohibit blind coordinate or command batches, direct state mutation, and network relays
- [ ] 8B.9 Capture and re-open the minimum legible A/B arrival pair, verifying that save, time, scene, NPC behavior sequence, and template match and that only actor attribution changes from player to moving NPC; return to exploration if any other material behavior diverges
- [ ] 8B.10 Run focused baseline/candidate formatter and classifier checks, decide whether their tests enter the submitted diff, run syntax and diff checks, and re-open `talk.py` plus the exact proposed diff against the actor-context contract
- [ ] 8B.11 Replace the stale title, body, and evidence captions through a `fable-5` medium-effort Agent/Workflow using only the exact proposed diff and inspected PR-visible images; omit the invalid route, local paths, private trials, and any test absent from the diff
- [ ] 8B.12 Obtain a fresh-context `review-erark-pr-artifacts` verdict with visibility and cumulative-prefix ledgers, route actionable prose revisions back through `fable-5`, and stop only at `PASS` or a concrete blocker
- [ ] 8B.13 Present the final local draft and inspected images to the user, then request separate authorization for evidence publication, branch push, and PR creation or editing before each outward action

## 8C. Three-Slot Tk Capture Concurrency

- [x] 8C.1 Add a three-slot supervisor that assigns an isolated Xvfb display, records owner/runtime/process metadata, and holds each lock for the complete command lifetime
- [x] 8C.2 Count pre-existing unsupervised `game.py` processes against the three-slot capacity so active legacy agents continue without interruption
- [x] 8C.3 Update the project skill and specification so one PR keeps baseline/candidate sequential while unrelated PR evidence tasks may occupy separate slots
- [ ] 8C.4 Migrate each still-running legacy evidence session to the slot supervisor only after its current game exits; do not kill or relaunch an active agent merely to adopt the new allocator
