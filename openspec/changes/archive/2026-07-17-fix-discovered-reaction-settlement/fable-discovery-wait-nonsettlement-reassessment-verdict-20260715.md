**Verdict: the lifecycle analysis is correct; WAIT non-settlement is coherent but not preferable; keep option 1 unless the maintainer explicitly chooses suppression, in which case option 2 is the right boundary. This is a semantics choice requiring maintainer confirmation.**

I verified every cited fact against the worktree at `356c1e86c`.

**1. Factual corrections — none.** All claims check out:
- NPC pass settles only `SHARE_BLANKLY`/`MOVE` (`character_behavior.py:169-178`); an NPC sitting in WAIT never re-enters `judge_character_status`.
- `judge_character_status_time_over` (lines 321-352) resets expired behavior to a fresh `Behavior()` without settling it. (Aside: the WAIT early-return at lines 313-315 tests behavior_id *after* the reset, so it's dead code — confirming there is no hidden WAIT settlement there either.)
- So the prior "later tick" description was wrong: a suppressed WAIT is never behavior-settled, ever. Confirmed.
- WAIT's direct effect is 9999/NOTHING (`Behavior_Effect.csv:8`), but a real settlement still runs talk lookup (which sets `have_shown_waiting_in_now_instruct`, `talk.py:597`) and pre/post event lookup. All three reactions carry effect 1721 (`Behavior_Effect.csv:172,186,187`), whose no-route fallback is WAIT/duration-1 (`StateMachine/default.py:58-63`).

**2. Coherence.** Non-settlement is a *defensible* contract — "the reaction is the meaningful act; the fallback WAIT is a scheduler placeholder" — because this WAIT only exists when pathfinding to the dormitory fails and lasts one minute. But it is not free: it silently discards the once-per-action WAIT talk line and any WAIT-keyed pre/post events for that NPC. It also creates an asymmetry the current design doesn't have: successor MOVE settles, successor WAIT becomes an unsettled ghost. The current contract ("whatever behavior the NPC ends up in after the reaction settles like any behavior") is the more uniform one.

**3. Easy ≠ preferable.** Easy: yes — one line, penalty 2. Preferable: no evidence supports it. No violated game rule has been named that settling the fallback WAIT breaks; the only motivation on the table is cosmetic (a "waiting" line right after the reaction text), and the per-instruct dedup flag already bounds that to one line. Under this skill's own standard, a change justified only by "the output looks slightly different" without a violated rule is not an acceptable fix. Recommendation: option 1, no production diff.

**4. If the maintainer nonetheless chooses suppression:** option 2 — extend the return in `character_see_h_and_move_to_dormitory` (`StateMachine/default.py:1325`) to also skip when the discoverer's post-panel behavior is WAIT. It sits at the logical owner (the discovery state machine decides what outer settlement sees after its panel), covers all three reaction branches at once, preserves MOVE settlement, and is the lowest penalty (2). Option 3 is the same semantics at penalty 9. Option 4 has equal penalty but rewrites the settlement contract for *every* state machine that ends in WAIT (e.g. `WAIT_5_MIN`) — broader than the question and touching semantics nobody asked about.

**5. Maintainer confirmation: required.** Upstream was inconsistent across the three reactions, so there is no unambiguous prior contract to "restore" — either choice sets public game semantics (WAIT talk/events present or absent after discovery). A prior Fable verdict already endorsed the current candidate; reversing it must be an explicit maintainer decision, not an inference from "it's easy." Do not edit production code until that answer is recorded in the OpenSpec change.
