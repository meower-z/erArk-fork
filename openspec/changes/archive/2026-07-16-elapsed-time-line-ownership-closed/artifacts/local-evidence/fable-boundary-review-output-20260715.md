PASS

**1. 两者都删是同一修复边界，且更小 — 采纳候选 2（penalty 27）。**
已核实：两种文本出自同一面板尾部、同一角色局部 `add_time`、同一条件分支的两个措辞；"该行动将持续X分钟"在正常路径同样以行为结束时刻结算，是结算阶段文本而非行动前预览。被违反的规则是"角色结算面板不拥有时间宣告，唯一实际经过时间由最外层 `game_update_flow` 显示一次"——两个措辞违反的是同一条规则。只删一个会让同一破坏规则在 exchange 兄弟路径上存活，这正是本 skill 明确禁止的边界。删除不是独立体验改动，无需拆分。附带说明：候选 2 会连带去掉 `add_time<=0` 时的尾部 `"\n"`，仅影响面板尾部一个空行，属可接受的边界内格式差异，不必为保留它加回两行（那会把 penalty 推回 29）。

**2. 可以执行，无需重跑 Tk。**
skill 规定行为变更候选只需一个代表性主案例的前后对照图；exchange 措辞删除是同一修复的兄弟案例，主案例（多条重复时间句 → 最外层一条）证据已存在。仅一项必做检查（非重跑）：用 `view_image` 重新打开已归档的 after 截图，确认画面中没有"该行动将持续"字样——若可见，该图与最终代码不符，不能作为 after 证据。

**3. Web 分支必要且正确，无 P2 必改项。**
已核实 `character_behavior.py` 在 Web 模式下不绘制结算面板（`if cache.web_mode: pass`），旧时间句到达玩家的唯一通道就是 `web_instruct_texts` + `emit_realtime_text(..., "instruct")`。若改用 `io_init.era_print`，Web 模式会转发到 `web_io.era_print` 的绘制元素通道——那是旧时间句从未使用的通道，会改变行为。当前显式分支精确保持"只从多条变一条，其余不变"。导入方面：`settle_behavior` 已在模块级导入 `web_server`（第 6 行），`update.py` 新增该导入不引入新的导入期副作用。

**4. 是，按此修改测试。**
`test_exchange_action_duration_preview_remains` 改为断言 exchange 路径面板中"该行动将持续"与"分钟过去了"均不出现（并相应改名和改中文注释，不再称"预览保留"）；`test_web_history_records_one_elapsed_time` 的恰好一次断言原样保留。测试为本地未跟踪文件，不入 diff。

**必须修改项汇总：** ① 把生产 diff 从候选 1 改为候选 2（settle_behavior 删 9 行、不加行，候选 1 的注释一并去掉）；② 按第 4 点更新本地测试并重跑；③ 重新目检既有 after 截图确认无"该行动将持续"字样。三项完成后该候选可进入 PR 草拟阶段。
