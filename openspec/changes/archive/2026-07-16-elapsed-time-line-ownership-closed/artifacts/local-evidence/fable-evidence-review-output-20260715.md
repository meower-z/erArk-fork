NEEDS_REVISION

## 逐张实际可见关键文字（全部经全分辨率裁剪后逐像素查验）

**A 修复前原始图**（2560×1350）
- `九开始自慰了` → `九:` 结算块（含 `群交经验 +1`）→ **`5分钟过去了`**
- `诗怀雅开始自慰了` → `诗怀雅:` 结算块（含 `群交经验 +1`）→ **`5分钟过去了`**（第二条，字面相同）
- `特蕾西娅开始自慰了` → `特蕾西娅:` 结算块（含 `群交经验 +1`）→ 底部还有一行被截断的疑似第三条
- 无 `该行动将持续` 入画。

**B 修复后群交块图 candidate-19**（2100×1479）
- `杜宾肛肠绝顶寸止`、`杜宾:` 结算块（`群交经验 +1`）
- `凯尔希:` 结算块（`群交经验 +1`）
- `====…=口 H中被发现 口=`、`可露希尔发现了你正在和凯尔希等多人群交，要怎么做?`、选项 `[1]/[4]/[5]`
- **全图无任何 `X分钟过去了`，无 `该行动将持续`。**

**C1 candidate-22-final-immediate**
- `可露希尔:` 结算块（`群交经验 +1`）之后为 **`该行动将持续10分钟`** → `结束群交模式`
- **画面内没有任何 `X分钟过去了`。**

**C2 candidate-23-final-immediate**
- 顶部依次为 **`该行动将持续10分钟`** → **`10分钟过去了`** → 场景分隔线与主界面。

**C3 run5-candidate-29（wheelup5 历史）**
- 顶部仅 **`10分钟过去了`** → 场景分隔线 → `时间:2019年 冬 29日 12点2分 …` 主界面。画面内无 `该行动将持续`。

## 五个问题的回答

1. **是。** A 同屏三个具名群交自慰结算块（每块含 `群交经验 +1`），两条完整字面 `5分钟过去了` 清晰可读，满足用户的修复前证据硬要求。
2. **是。** B 同屏两个具名群交结算块，块间与块尾均无角色级 `X分钟过去了`，也无 `该行动将持续`。
3. **实质上否。** C1 含 `该行动将持续10分钟` 且不含合并行；C2 同屏既有合并 `10分钟过去了` 又有 `该行动将持续10分钟`。只有 C3 字面满足"有合并行、无该措辞"——但那是滚动截取造成的不入画，不是构建里没有：**C2 出自更晚的 run10 rebased 构建，证明拍摄时的代码仍会打印该措辞**。拿 C3 当"最终代码已删除该措辞"的证据是用取景掩盖不一致，不能接受。
4. **可以。** 只要图注明确写出"A 摄自最初问题现场存档，仅作问题示例，与修复后截图非同存档同路线"，A+B 这一对是诚实的问题/修复对照。A/B 严格同存档对比的缺口已由披露覆盖；瓶颈不在 A，在 C。
5. **A+B 两图诚实可用，但有条件。** B 只证明"角色级时间行已消失"，没有任何一张与最终代码一致的图证明"最外层只出现一条净 `X分钟过去了` 且无 `该行动将持续`"。两条路：
   - **推荐：** 用最终代码重放 run10 同一路线，只补拍一张最终结算帧（预期：合并 `10分钟过去了`，无第二措辞）。这不是"为删措辞重跑全程"，是补一帧缺失的核心后置状态。
   - **不重跑的底线方案：** PR 只放 A+B，且必须在图注明确披露"修复后截图摄于仍显示`该行动将持续`的中间构建，故未附最终结算帧；单条合并行为由随 diff 提交的自动化断言/源码保证"。该保证要成立，对应断言必须真的在提交 diff 里（不在 diff 里就不能引用）。任何情况下 C1/C2 不得作为最终代码证据入 PR；C3 也不得入 PR。

## 最小可接受图片集合

**A + B + 一张最终代码下的最终结算帧（新拍）**。若用户坚持不重跑：A + B，配上述强制披露，且合并行断言在提交 diff 内——否则该主张无 PR 可见支撑，应视为 BLOCKED 级缺口。

## 精确披露要求

1. A 图注：`修复前截图摄自玩家最初报告的问题现场存档（群交自慰，三名角色各自输出"5分钟过去了"），仅示意问题，与修复后截图不同存档。`
2. B 图注：`修复后同类群交结算：角色结算块后不再逐角色输出"X分钟过去了"。`
3. 若采用不重跑方案，追加：`修复后截图摄于删除"该行动将持续X分钟"之前的构建；该措辞的删除与最外层单条"X分钟过去了"由本 PR 附带的测试断言覆盖。`
