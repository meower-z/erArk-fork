**结论：代码 PASS，文案 PASS，上游 PR artifact 仅因证据缺失 BLOCKED。不需要再改生产代码或 PR 文案。**

（说明：本会话未启用文件/Bash 工具，无法独立打开仓库；以下审计基于提交的完整 diff 文本、draft 文本，以及请求方声明已在 base 源码中直接确认的三处调用点。）

## 1. 代码审计 — PASS

逐层推演新语义：

- 外层进入：`caller_depth=0` → 标志置 1；嵌套进入（`recover_from_unconscious_h` → `game_update_flow(5)`）：`caller_depth=1` → 置 2；第三层被 `>= 2` 守卫拦截；内层 `finally` 恢复 1，外层 `finally` 恢复 0。每层只撤销自己那一层，不变量成立。
- 旧代码缺陷确实存在：内层 `finally` 固定清零后，外层仍在运行而标志为 0，后续任何嵌套调用都会以错误的深度基准通过守卫。
- 异常路径：嵌套调用中抛异常时，旧代码把标志清成 0（错误地抹掉外层），新代码恢复 1（正确）。修复在异常路径上严格更好。
- 最小性核对：仅 `Script/Design/update.py`，5+/4-；守卫阈值 `>= 2` 未动；try 体内时间推进 → 角色结算 → 聚焦的顺序未动；注释为中文，符合项目规范。没有更小的正确修法（单纯 `-= 1` 递减与保存/恢复等价，但保存/恢复在异常中途状态被外部改写时更稳，且行数相同）。

## 2. 文案审计 — PASS

- **无测试内容**：draft 中没有任何自动化测试、本地验证过程、调查记录或文件清单。✓
- **关键句核对**：「后续的防重入判断失去了正确的深度依据」是由旧代码直接推出的条件/不变量陈述——内层返回后标志=0 而外层仍在运行，此后守卫读到的深度必然错误。它没有声称观察到第三次进入，也没有发明任何 A/B 或运行时现象。✓
- 嵌套路径的表述（角色结算 → `recover_from_unconscious_h()` → `game_update_flow(5)`）与请求方确认的三处源码位置一致，是唯一超出 diff 本身的事实性声明，且已被 base 源码确认支持。
- 顺序为问题 → 原因 → 修复，全部使用代码中的真实标识符，无自造术语，无非目标清单，标题与正文一致且准确（「深度达到 2 时拦截更深的嵌套调用」与 `>= 2: return` 语义吻合）。

## 3. 上游 PR artifact — BLOCKED（仅证据）

这是行为变更候选（守卫深度语义修复），按项目规则需要一组来自真实 Tk 的代表性前后对比图。当前 PR-facing 证据为零，且真实 Tk 探索未进入修改分支，无法诚实构造 A/B。因此：

- **阻断原因唯一**：缺少真实 Tk 前后证据。
- **不阻断的部分**：production diff 和 PR draft 均已达到可提交质量，无需修订。
- 允许的下一步仍是用户此前授权的范围：提交到 fork side branch 供审核；创建上游 PR 需先补齐 Tk 证据（或由用户明确豁免）。
