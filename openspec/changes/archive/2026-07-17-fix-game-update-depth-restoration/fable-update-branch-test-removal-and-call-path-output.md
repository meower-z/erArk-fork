已自行核验：`fb10be12a` 的父提交是 `23188ce1a`，本身只删除 `tests/test_game_update_depth_restoration.py`（157 行）；`upstream/master...fb10be12a` 的 diff 仅 `Script/Design/update.py`（+5/−4）；tip 树中无 `tests/`；该文件 blob 哈希与 `bc1bfb44e` 完全一致（`fcd4340d`）。调用链七步逐一在源码中确认存在。逐项裁定如下。

**1. 分支处理 — 裁定：最小且正确。** 保留用户点击 "Update branch" 产生的 merge、在其上追加一个只删测试文件的普通提交，是唯一既不改写用户主动生成的历史、又能清除测试文件的做法。已验证最终拟提交 diff（相对 `upstream/master`）只剩 `Script/Design/update.py` 一个生产文件，且与已审计通过的 `bc1bfb44e` 版本逐字节相同。无需其他处理。

**2. 术语 — 裁定：称"条件触发的间接递归"最准确，同时也是一种重入。** 用户观察正确：`game_update_flow()` 没有直接调用自己，所以不是直接递归。但它在同一调用栈上、同步地经由七步链条（时间推进 → 角色结算 → 玩家实时结算 → 睡奸结算 → 惊醒判定 → 无意识恢复 → `game_update_flow(5)`）回到自身——外层调用尚未返回时函数被再次进入。这在术语上是**间接递归**（A 经 B、C…回到 A）；因为"函数在前一次执行未完成时被再次进入"，也构成**重入**。对用户的一句话解释：不是"自己调自己"，而是"它启动的结算流程在特定条件下（睡奸中对象被吵醒）会反过来再启动一次它"。不要称"死循环"——`>= 2` 守卫把深度封顶，本修复解决的是嵌套返回后深度被误清零。

**3. 防护与根因分离 — 裁定：是，且必须分离。** 深度保存/恢复修复对该生产循环是正确防护：内层返回时只撤销自己那一层，外层深度不再被清零，守卫的判断依据恢复正确。但 Tk 99/97 号档证据只见最外层 update、从未进入嵌套路径，因此该循环只能作为"生产代码中存在的条件式路径"陈述，**不能**作为用户截图连续高潮现象的已确认根因。两者在所有记录和 PR 文案中保持分离。

**4. push 授权范围 — 裁定：匹配。** 用户要求"把 branch 中的自动化测试文件移除"，该 branch 是 fork 上的远端分支；在保留 merge 的前提下，将 `fb10be12a` 普通 fast-forward push 到同名 fork 分支是履行该指令的最小动作，授权范围覆盖它。边界：仅此一次、仅该分支、普通 push（不 force，若远端 tip 已不是 `23188ce1a` 则停下重新确认）；创建/编辑 PR 不在此授权内。Fable 依规不授予权限，本项只裁定范围匹配。

**5. PR draft — 裁定：无需修改（PASS，publication_state: local-review-ready）。** 现有 draft 措辞已合规："生产代码中存在嵌套路径……会再次调用 `game_update_flow(5)`" 是任何上游 reviewer 读 upstream 源码即可验证的静态事实陈述，全文没有"已复现""玩家遇到"之类的运行时声明，也没有引用任何本地测试或截图。问题→修复顺序正确，术语（`game_update_flow`、`recover_from_unconscious_h`）均为代码内标识符，前缀可自立。保持现状即可；唯一红线是后续任何修订都不得把这条条件式路径写成已在玩家路线中观测到的故障。
