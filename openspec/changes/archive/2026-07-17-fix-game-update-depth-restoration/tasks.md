## 1. 可失败的深度生命周期回归

- [x] 1.1 建立同一外层更新依次调用两个嵌套更新的真实入口回归，先证明当前实现会在第一个嵌套返回后错误归零。
- [x] 1.2 建立嵌套更新正常返回和抛出异常的回归，分别断言返回调用者深度为 1、最外层最终深度为 0。
- [x] 1.3 建立当前深度达到 2 时更深更新仍被拒绝且不改变现有深度的回归。

## 2. 成对恢复实现与验证

- [x] 2.1 在 `game_update_flow` 入口保存调用者深度，并在已获准调用的 `finally` 中恢复该值，保持现有准入条件和流程顺序不变。
- [x] 2.2 运行三组聚焦回归和 Python 语法检查，确认正常、异常、先后嵌套及 depth≥2 路径全部满足规格。
- [x] 2.3 记录本地自动化验证结果但不把此前置修复单独标为上游发布就绪；发布审计必须由实际包含该修复的消费者 diff 和真实 Tk 证据共同覆盖。

## 发布限制

本 change 的实现任务全部完成不表示它可以单独发布。它仍被阻塞在独立上游发布边界之外；只有实际消费者 change 的拟提交 diff 包含此修复，并由该消费者的真实 Tk 前后证据覆盖稳定的最外层身份后，才能随消费者进入发布审计。

## 归档收尾（2026-07-17）

本 change 已完结并归档：对应上游 PR #216「修复 game_update_flow 嵌套调用返回后运行深度被误清零的问题」已合并（上游提交 72e28051e）。游戏更新嵌套深度成对恢复的前置修复已随该 PR 直接交付上游，目录仅作历史记录保留。
