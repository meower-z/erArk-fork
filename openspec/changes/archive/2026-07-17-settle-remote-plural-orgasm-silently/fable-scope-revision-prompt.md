/investigate-game-bug

请作为 erArk 上游 PR 文案作者，按玩家审查意见修订下面这份尚未提交的 PR draft。不要改代码，不要重新做调查；只从给定的已核验规则、精确 diff 和公开证据，产出一版完整中文定稿。用户的核心纠正是：当前“问题”段把一个测试样例写成了 bug 定义，scope 过窄；必须如实描述整个规则范围，小满/咖啡馆/等待五分钟只能作为“验证”中的代表性样例。

已核验的真实 bug scope：

1. 受影响行为是所有 `plural_orgasm_2` 至 `plural_orgasm_11`，即双重至十一重绝顶；适用于任意 NPC、任意地点和任意能生成这些二段行为的玩法来源，不限于小满、咖啡馆、等待指令或群交。
2. 当这些多重绝顶在 NPC 与玩家不在同一地点时生成，它们因为缺少 997，不会进入角色的远程必须结算列表。远程二段行为路径只处理 must-show / must-settle 后返回，因此旧行为继续留在 `second_behavior` 队列。
3. 之后该 NPC 进入玩家附近并完成当前行为时，这条旧多重绝顶才会显示并结算。于是玩家可能在等待、移动后的结算或其他本身与绝顶无关的操作后，突然看到此前异地 NPC 的多重绝顶提示。这里“进入玩家附近并完成当前行为”是机制条件，不要把某一个具体动作写成必要条件。
4. 修复只在十个 CSV 行末尾追加 997。对这十个行为的现有远程二段路径，这会使其进入必须结算列表而不进入必须显示列表：数值正常结算、旧队列清除，不向玩家显示异地口上；NPC 与玩家同处时的原有显示路径不变。
5. 不得把 scope 扩成所有绝顶衍生行为；`extra_orgasm`、喷乳、漏尿不在本提交中。PR 不需要列 non-goal，但表述必须始终限定为“双重至十一重绝顶”或“这些多重绝顶”。

精确提交 diff：`data/csv/Behavior_Effect.csv` 中 `plural_orgasm_2` 至 `plural_orgasm_11` 十行，各自在原有效果末尾追加 `997`；没有 Python、测试或其他生产文件。

公开证据只有一组代表性 Tk 主 A/B：

- `before.png`：咖啡馆执行等待五分钟后的结算底部出现黄色“小满双重绝顶”。
- `after.png`：同一存档、随机种子、玩家路线和对应时点回到咖啡馆，没有该延迟文本。
- 这只是代表性验证，不定义 bug scope。不要写 12:29 来源、private trace、附近凯尔希对照、本地测试、路径或未提交材料。

写作要求：

- 沿用已合并 #210/#211/#206 的高度与简洁度，结构为“问题、修复、验证”。
- 标题和问题段第一句必须直接陈述一般玩家可见问题，不以小满/咖啡馆或内部 ID 定义 bug。
- 问题段按“一般可见失败 → 确认原因”顺序；原因可提这十个多重绝顶缺少 997，但不要用样例救援一般规则。
- 验证段才引入小满/咖啡馆，明确它是一条代表性路线。
- 997 的保证必须限定在这十个多重绝顶行为的现有远程二段结算路径，不能泛化为所有效果。
- 保留 `BEFORE_URL` / `AFTER_URL` 占位，并为两张图写只描述图中可见内容的中文 alt/caption。
- 不写 local-only 测试、trace、OpenSpec、调查过程、文件路径清单、独立 non-goal 清单或 outward-action 建议。

当前 draft（仅供保持已经通过的验证/caption事实，不得保留其过窄的问题定义）：

```markdown
# PR Draft

## Title

修复：玩家不在场时多重绝顶结算被延迟显示

## Body

## 问题

实际游玩中，玩家在咖啡馆执行等待五分钟后，结算文本底部会出现一条延迟的黄色“小满双重绝顶”。原因是 `data/csv/Behavior_Effect.csv` 中 `plural_orgasm_2` 至 `plural_orgasm_11` 十个多重绝顶结算效果缺少 997 标记，玩家不在场时未被即时结算，残留到玩家后续的结算输出中。

## 修复

为上述十行各追加 997 标记。在这十个多重绝顶行为所在的远程二段结算路径中，997 使该效果进入必须结算列表而不进入必须显示列表；补上标记后，多重绝顶在发生当刻即被结算完毕，不再残留到玩家之后的结算中。玩家在场时的显示逻辑不在本次修改范围内，保持原样。

## 验证

同一存档、同一随机种子、同一玩家路线的修复前后对比：

![修复前](BEFORE_URL)

![修复后](AFTER_URL)

## Image alt/captions

- BEFORE_URL: 修复前：在咖啡馆执行等待五分钟后，结算文本底部出现延迟的黄色“小满双重绝顶”。
- AFTER_URL: 修复后：同一路线的对应时点回到咖啡馆界面，不再出现该延迟的多重绝顶文本。
```

请输出唯一一版完整定稿：标题、正文、两张图片的 alt/caption。不要附修订说明。

