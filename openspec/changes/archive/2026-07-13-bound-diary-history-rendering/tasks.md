## 1. 复现与边界测试

- [x] 1.1 构造跨数百游戏日、数万行动片段的 `daily_intsruce`，记录现有面板首次绘制的文本大小与耗时，确认其默认消费全部未导出历史
- [x] 1.2 新增红测：首次打开只绘制当前日的有界第一页，旧日期和同日溢出内容需显式导航后才进入 draw payload
- [x] 1.3 新增旧存档红测：有标准日期分隔、无可识别分隔、单日超大三种平面列表均可有界浏览且不丢条目
- [x] 1.4 新增导出红测：浏览范围有限但导出包含全部未归档历史与插入文本；写入失败时缓存不清空

## 2. 有界视图实现

- [x] 2.1 为平面 `daily_intsruce` 实现非破坏性的日期段/页面视图适配器，当前日默认定位，无法分组的旧内容归入可访问的历史页
- [x] 2.2 为日记面板增加上一页/下一页或加载更多控制，并限制每次传给 Tk/Web 绘制层的条目数或字符数
- [x] 2.3 将页面文本构建改为片段收集后一次 `join`，禁止遍历全部历史做循环 `str +=`
- [x] 2.4 保持导出全量语义，使用逐项写入；仅在完整写出成功后清空 `daily_intsruce`

## 3. 验证与兼容

- [x] 3.1 在 Tk 与 Web 适配层分别验证大历史首次打开仍能显示导航控件，不生成全量单次绘制元素
- [x] 3.2 真实旧存档烟雾验证：读档、查看今天、翻到旧日、导出、再次进入日记，确认记录和文件内容一致
- [x] 3.3 更新数据工作流/日记说明，明确“缓存范围是上次成功导出以来，默认视图范围是当前日有界页”
- [x] 3.4 `openspec validate --strict`、相关测试与 `git diff --check` 通过
