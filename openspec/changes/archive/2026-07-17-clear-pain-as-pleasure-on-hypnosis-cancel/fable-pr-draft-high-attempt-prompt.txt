/investigate-game-bug
/review-erark-pr-artifacts

请作为 erArk 上游 PR 文案 writer，以当前 skill 要求的 high effort 生成 PR #213 最终本地审阅稿。只授权写本地 draft，不授权 commit、push、上传图片或编辑 GitHub PR。

写作边界：
- 读者玩过 erArk，但不了解这个 bug；按“玩家可见问题 → 已确认原因 → 最终修复 → Tk 验证”排列，每个累计前缀当场可理解。
- 只使用下面的最终 production diff 和三张准备发布的真实 Tk 图片。不要写本地测试、命令、工作树、OpenSpec、Fable、maintainer 讨论、旧候选、文件清单或非目标列表。
- 不使用“否则玩家无法去掉”作为依据；实际依据是字段加入后漏接旧的直接解除生命周期，以及两条退出路径应清同一组五项持续性子状态。
- 不写内部效果编号 `1213`/`489`、函数标识符或冗余的未改动范围清单。
- 五项子状态枚举应保留；“该效果实际继续生效”已有 production reader/settlement 支持，不是仅凭截图推断。
- 图片尚未获准公开，必须保留 `{{BEFORE_URL}}`、`{{BASELINE_AFTER_URL}}`、`{{CANDIDATE_AFTER_URL}}`；这是 local-review-ready，不是 publication-ready。
- 下面现有正文已经通过一次完整事实/证据审查，并按唯一两项 findings 精简。优先保留已通过文字；只有 high-effort writer 发现明确的前缀、准确性或可读性问题时才改，并在输出的 before/after diff 中解释。

最终 production diff：
- 相对 PR base 只涉及 `Script/Design/hypnosis_state.py` 与 `Script/Settle/default.py`。
- 新共用 helper 清除 `increase_body_sensitivity`、`blockhead`、`active_h`、`pain_as_pleasure`、`roleplay`。
- 主动`解除催眠`与`熟睡`都调用 helper；睡眠原行为不变，直接解除新增清除 `pain_as_pleasure`。
- 不提交自动化测试。

已确认原因：
- 直接`解除催眠`的子状态清理代码早于 `pain_as_pleasure` 字段；字段后来加入时没有补进旧清理。
- 后来建立的`熟睡`清理从一开始就包含它，因此两份近似列表发生漏项。

准备发布的 Tk 证据：
- before：同一存档，状态栏 `<催眠(200%):心控(敏感)(痛→快感)>`，同画面可见 `[4004]解除催眠`。
- baseline-after：上游当前版本执行同一指令后 `<催眠(200%)(痛→快感)>`。
- candidate-after：最终窄候选执行同一指令后 `<催眠(200%)>`。
- before 在 A/B 逐像素相同；三图来自真实 Tk、相同存档、相同路线和逐帧视觉操作。完整 provenance 仅本地，不写进 PR。

当前已审查稿：

标题：`修复：解除催眠后"苦痛快感化"未被一并解除`

```markdown
## 问题

对已被施加`苦痛快感化`的干员主动使用`[4004]解除催眠`后，`心控`等其他催眠子状态都已结束，但状态栏仍残留`(痛→快感)`，该效果实际继续生效。

作为对比，干员`熟睡`自然结束催眠时，`苦痛快感化`会随其他子状态一并解除。设计上两条退出路径应当一致：结束催眠时，同一组持续性心体催眠子状态应当全部结束。

## 原因

主动`解除催眠`的子状态清理代码写于 `pain_as_pleasure` 字段加入之前；该字段后来加入时没有补进这份旧的清理列表。而更晚建立的`熟睡`清理从一开始就包含它。于是两处各自维护了一份几乎相同的清理列表，其中主动`解除催眠`少了`苦痛快感化`这一项。

## 修复

新增一个共用函数，专门负责结束五项持续性心体催眠子状态：`敏感度提升`、`木头人`、体控`逆推`、`苦痛快感化`、`角色扮演`。主动`解除催眠`和`熟睡`都改为调用它，这份列表只表达一次，两条路径不会再各自漏项。

`熟睡`的行为不变；主动`解除催眠`补上了对`苦痛快感化`的清除，与`熟睡`对齐。

## 验证

用同一份存档，以凯尔希作为代表案例走同一操作流程：

- 操作前，凯尔希状态栏显示 `<催眠(200%):心控(敏感)(痛→快感)>`，同一画面可见`[4004]解除催眠`指令。

[![操作前：状态栏含 痛→快感，可见解除催眠指令]({{BEFORE_URL}})]({{BEFORE_URL}})

- 上游当前版本：执行`[4004]解除催眠`后，状态栏仍显示 `<催眠(200%)(痛→快感)>`，效果残留。

[![上游当前版本：解除催眠后 痛→快感 残留]({{BASELINE_AFTER_URL}})]({{BASELINE_AFTER_URL}})

- 本 PR：同样操作后，状态栏显示 `<催眠(200%)>`，`痛→快感`已随其他子状态一并解除。

[![本 PR：解除催眠后 痛→快感 一并解除]({{CANDIDATE_AFTER_URL}})]({{CANDIDATE_AFTER_URL}})
```

请严格输出：
1. `标题`。
2. `最终正文`（完整 Markdown）。
3. `before/after diff`：列出相对当前已审查稿的全部变化；若无变化，明确写“无”。这部分不进入 PR 正文。
4. `publication_state: local-review-ready`。
