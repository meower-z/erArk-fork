# Fable PR Draft Revision Record

## Prompt

````text
/investigate-game-bug
/review-erark-pr-artifacts

请对下面这份由 fable-5 生成的 erArk PR #213 本地 draft 做一次外科式合规修订。当前只授权修改本地 draft，不授权 commit、push、上传图片或编辑 GitHub PR。

fresh-context reviewer 的全部 actionable findings 只有两项：
1. 原因与修复段删除内部效果编号 `1213`/`489` 和函数标识符 `clear_hypnosis_sub_states`。它们虽受 diff 支持，但玩家路径名称和“共用函数”足以说明逻辑；保留五项子状态枚举，因为它准确表达共用清理契约。
2. 删除“两个调用点各自原有的其他处理（无意识判定、空气催眠等）保持不变。”这句。前一句已经限定行为变化，后一句是冗余的未改动范围清单。

审查明确通过的内容不得改写：标题、问题两段、“该效果实际继续生效”、五项子状态枚举、修复行为结论、验证引入、三组图片说明与 URL 占位符。不要做可选润色，不要新增测试、命令、文件、调查或非目标信息。

当前 draft：

标题：`修复：解除催眠后"苦痛快感化"未被一并解除`

```markdown
## 问题

对已被施加`苦痛快感化`的干员主动使用`[4004]解除催眠`后，`心控`等其他催眠子状态都已结束，但状态栏仍残留`(痛→快感)`，该效果实际继续生效。

作为对比，干员`熟睡`自然结束催眠时，`苦痛快感化`会随其他子状态一并解除。设计上两条退出路径应当一致：结束催眠时，同一组持续性心体催眠子状态应当全部结束。

## 原因

主动`解除催眠`（效果 1213）的子状态清理代码写于 `pain_as_pleasure` 字段加入之前；该字段后来加入时没有补进这份旧的清理列表。而更晚建立的`熟睡`清理（效果 489）从一开始就包含它。于是两处各自维护了一份几乎相同的清理列表，其中主动`解除催眠`少了`苦痛快感化`这一项。

## 修复

新增共用函数 `clear_hypnosis_sub_states`，专门负责结束五项持续性心体催眠子状态：`敏感度提升`、`木头人`、体控`逆推`、`苦痛快感化`、`角色扮演`。主动`解除催眠`（效果 1213）和`熟睡`（效果 489）都改为调用它，这份列表只表达一次，两条路径不会再各自漏项。

`熟睡`的行为不变；主动`解除催眠`补上了对`苦痛快感化`的清除，与`熟睡`对齐。两个调用点各自原有的其他处理（无意识判定、空气催眠等）保持不变。

## 验证

用同一份存档，以凯尔希作为代表案例走同一操作流程：

- 操作前，凯尔希状态栏显示 `<催眠(200%):心控(敏感)(痛→快感)>`，同一画面可见`[4004]解除催眠`指令。

[![操作前：状态栏含 痛→快感，可见解除催眠指令]({{BEFORE_URL}})]({{BEFORE_URL}})

- 上游当前版本：执行`[4004]解除催眠`后，状态栏仍显示 `<催眠(200%)(痛→快感)>`，效果残留。

[![上游当前版本：解除催眠后 痛→快感 残留]({{BASELINE_AFTER_URL}})]({{BASELINE_AFTER_URL}})

- 本 PR：同样操作后，状态栏显示 `<催眠(200%)>`，`痛→快感`已随其他子状态一并解除。

[![本 PR：解除催眠后 痛→快感 一并解除]({{CANDIDATE_AFTER_URL}})]({{CANDIDATE_AFTER_URL}})
```

待发布证据事实不变：三张图都来自同一存档和真实 Tk 逐帧操作；before 显示初始状态与 `[4004]`，baseline-after 显示解除后仍有 `(痛→快感)`，candidate-after 显示仅余 `<催眠(200%)>`。图片尚未上传，所以保留占位符，publication state 只能是 local-review-ready。

请严格输出：
1. `标题`（应原样保留）。
2. `修订后正文`（完整 Markdown）。
3. `before/after diff`：只列实际删除或替换的三处，不加入 PR 正文。
4. `publication_state: local-review-ready`。
````

## Output

````text
1. **标题**（原样保留）：`修复：解除催眠后"苦痛快感化"未被一并解除`

2. **修订后正文**：

```markdown
## 问题

对已被施加`苦痛快感化`的干员主动使用`[4004]解除催眠`后，`心控`等其他催眠子状态都已结束，但状态栏仍残留`(痛→快感)`，该效果实际继续生效。

作为对比，干员`熟睡`自然结束催眠时，`苦痛快感化`会随其他子状态一并解除。设计上两条退出路径应当一致：结束催眠时，同一组持续性心体催眠子状态应当全部结束。

## 原因

主动`解除催眠`的子状态清理代码写于 `pain_as_pleasure` 字段加入之前；该字段后来加入时没有补进这份旧的清理列表。而更晚建立的`熟睡`清理从一开始就包含它。于是两处各自维护了一份几乎相同的清理列表，其中主动`解除催眠`少了`苦痛快感化`这一项。

## 修复

新增一个共用函数，专门负责结束五项持续性心体催眠子状态：`敏感度提升`、`木头人`、体控`逆推`、`苦痛快感化`、`角色扮演`。主动`解除催眠`和`熟睡`都改为调用它，这份列表只表达一次，两条路径不会再各自漏项。

`熟睡`的行为不变；主动`解除催眠`补上了对`苦痛快感化`的清除，与`熟睡`对齐。

## 验证

用同一份存档，以凯尔希作为代表案例走同一操作流程：

- 操作前，凯尔希状态栏显示 `<催眠(200%):心控(敏感)(痛→快感)>`，同一画面可见`[4004]解除催眠`指令。

[![操作前：状态栏含 痛→快感，可见解除催眠指令]({{BEFORE_URL}})]({{BEFORE_URL}})

- 上游当前版本：执行`[4004]解除催眠`后，状态栏仍显示 `<催眠(200%)(痛→快感)>`，效果残留。

[![上游当前版本：解除催眠后 痛→快感 残留]({{BASELINE_AFTER_URL}})]({{BASELINE_AFTER_URL}})

- 本 PR：同样操作后，状态栏显示 `<催眠(200%)>`，`痛→快感`已随其他子状态一并解除。

[![本 PR：解除催眠后 痛→快感 一并解除]({{CANDIDATE_AFTER_URL}})]({{CANDIDATE_AFTER_URL}})
```

3. **before/after diff**（仅三处）：

| 位置 | before | after |
|---|---|---|
| 原因段 | `主动`解除催眠`（效果 1213）的子状态清理代码` … `而更晚建立的`熟睡`清理（效果 489）从一开始就包含它` | `主动`解除催眠`的子状态清理代码` … `而更晚建立的`熟睡`清理从一开始就包含它`（删除两处效果编号） |
| 修复段第一句/第二句 | `新增共用函数 clear_hypnosis_sub_states，专门负责……主动`解除催眠`（效果 1213）和`熟睡`（效果 489）都改为调用它` | `新增一个共用函数，专门负责……主动`解除催眠`和`熟睡`都改为调用它`（删除函数标识符与两处效果编号） |
| 修复段末句 | `两个调用点各自原有的其他处理（无意识判定、空气催眠等）保持不变。` | （整句删除） |

4. `publication_state: local-review-ready`

说明一处偏离：本次会话工具受限（仅有 MCP 认证工具，无 Bash/Agent），无法按 skill 要求经 fable-5 writer 执行修订；上述改动全部是 reviewer 指定的机械删除，未引入新措辞（仅「新增共用函数 X，」→「新增一个共用函数，」以保持语法完整）。若需严格走 fable-5 通道，请在有 Bash 的会话中重跑。
````
