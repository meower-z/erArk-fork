# Fable PR Draft Writer Record

## Prompt

````text
/investigate-game-bug
/review-erark-pr-artifacts

请作为 erArk 上游 PR 文案 writer，使用中文重写 PR #213 的标题、正文和证据图片说明。当前只需要供用户审阅的本地 draft，不授权 commit、push、上传图片或修改 GitHub PR。

必须遵守：
- 读者玩过 erArk，但不了解这个 bug；标题先说玩家可见问题。
- 正文按“可见问题 → 已确认原因 → 最终修复 → Tk 验证”排列，每个累计前缀必须当场可理解。
- 只写最终提交 diff 能支持的事实和准备发布的真实 Tk 图片。不要写本地测试、命令、工作树、OpenSpec、Fable、调查过程、被拒方案、文件清单或独立非目标列表。
- 不要重复 maintainer 的错误字面理由“否则玩家无法去掉”；效果 1230 本身是 toggle，这不是修复依据。
- 不要声称旧宽候选截图证明当前 diff。使用下面新的窄候选 A/B。
- 图片还未获准公开，正文用 `{{BEFORE_URL}}`、`{{BASELINE_AFTER_URL}}`、`{{CANDIDATE_AFTER_URL}}` 作为明确占位符；它是 local-review-ready draft，不是可直接粘贴的 publication-ready 文本。
- 尽量保留当前 draft 已经合适的四个标题和简洁高度，但由于语义完全翻转，问题、原因、修复和验证内容必须随之重写。输出后列出相对当前 draft 的必要修改对照，说明为何不能只改一两句话。

当前远端 PR：
- title: `修复：干员熟睡后"苦痛快感化"被错误解除`
- base: `master`
- head: `e1a9378b140f99cd62f9c678c3a1113981e4e342`
- 当前正文原文：

```markdown
## 问题

当一名已被施加`苦痛快感化`的干员自行入睡进入`熟睡`时，她的`痛→快感`效果会被错误清除。

作为对比，对干员主动使用`解除催眠`时，`苦痛快感化`是保留的。设计上两种情况应当一致：无论干员`熟睡`还是被主动`解除催眠`，`苦痛快感化`都应保留。

## 原因

`熟睡`和主动`解除催眠`都会结束同一组心体催眠子状态：`敏感度提升`、`木头人`、体控`逆推`、`角色扮演`。这四项在两种情况下的处理本来就一致。问题在于，`熟睡`时执行的清理列表里意外多包含了一项`苦痛快感化`，所以只有睡觉这一种情况会把它一并清掉。

## 修复

把两处相同的四项子状态清理提取为一个共用函数，`熟睡`和`解除催眠`都改为调用它。这样两种情况都只结束上述四项，`苦痛快感化`不再被混入`熟睡`的清理。`解除催眠`的行为不变，`熟睡`与之对齐。

## 验证

用同一份存档，以凯尔希作为代表案例走完整流程：

- 操作前，凯尔希状态栏显示 `<催眠(200%):心控(敏感)(痛→快感)>`。
- 上游当前版本：凯尔希入睡后，状态栏含`熟睡`与 `<催眠(200%)>`，`痛→快感`已丢失。
- 本 PR：同样入睡后，状态栏含`熟睡`与 `<催眠(200%)(痛→快感)>`，效果保留。
```

maintainer 反馈后的最终语义与根因：
- 玩家可见问题是：对带有 `苦痛快感化` 的干员主动使用 `[4004]解除催眠` 后，心控等子状态已经结束，但状态栏仍显示 `(痛→快感)`。
- 直接 `解除催眠` 的清理代码写于 `pain_as_pleasure` 字段加入之前；该字段后来加入时没有补进旧清理。睡眠效果 489 后来建立时已包含该字段。因此旧不对称是新增字段漏接直接解除生命周期。
- 违反的规则：结束催眠时，两条退出路径应结束同一组五个持续性心体催眠子状态，且这份列表只表达一次。

最终拟提交 production diff（没有提交测试）：
- 新增 `clear_hypnosis_sub_states(target_character_id)`，只把 `increase_body_sensitivity`、`blockhead`、`active_h`、`pain_as_pleasure`、`roleplay` 清零。
- 直接解除效果 1213 和睡眠效果 489 都调用该 helper。
- 睡眠原有行为不变；直接解除新增清除 `pain_as_pleasure`。
- caller 各自的无意识边界、异常 flag、空气催眠和二段结算均保持原位。

准备作为 PR 证据的最新真实 Tk 图片（尚未上传）：
1. `before`：2000x930，操作前状态栏为 `<催眠(200%):心控(敏感)(痛→快感)>`，同一画面可见 `[4004]解除催眠`。baseline/candidate 此帧逐像素相同。
2. `baseline-after`：2000x930，上游当前版本执行 `[4004]解除催眠` 后显示 `<催眠(200%)(痛→快感)>`。
3. `candidate-after`：2000x930，窄候选执行同一操作后显示 `<催眠(200%)>`。
两侧来自同一 upstream base、同一存档、同一 Tk 几何和同一逐帧玩家操作；before 与指令结果帧逐像素一致。完整路线和存档仅本地保留，不写进 PR。

请严格输出：
1. `标题`：一行。
2. `正文`：完整 Markdown，图片采用可点击缩略图格式 `[![说明]({{..._URL}})]({{..._URL}})`。
3. `必要修改对照`：只列相对当前远端 draft 必须改变的标题/段落及原因，不把这部分放进 PR 正文。
4. `publication_state: local-review-ready`。
````

## Output

````text
`标题`：

修复：解除催眠后"苦痛快感化"未被一并解除

`正文`：

```markdown
## 问题

对已被施加`苦痛快感化`的干员主动使用`[4004]解除催眠`后，`心控`等其他催眠子状态都已结束，但状态栏仍残留`(痛→快感)`，该效果实际继续生效。

作为对比，干员`熟睡`自然结束催眠时，`苦痛快感化`会随其他子状态一并解除。设计上两条退出路径应当一致：结束催眠时，同一组持续性心体催眠子状态应当全部结束。

## 原因

主动`解除催眠`（效果 1213）的子状态清理代码写于 `pain_as_pleasure` 字段加入之前；该字段后来加入时没有补进这份旧的清理列表。而更晚建立的`熟睡`清理（效果 489）从一开始就包含它。于是两处各自维护了一份几乎相同的清理列表，其中主动`解除催眠`少了`苦痛快感化`这一项。

## 修复

新增共用函数 `clear_hypnosis_sub_states`，专门负责结束五项持续性心体催眠子状态：`敏感度提升`、`木头人`、体控`逆推`、`苦痛快感化`、`角色扮演`。主动`解除催眠`（效果 1213）和`熟睡`（效果 489）都改为调用它，这份列表只表达一次，两条路径不会再各自漏项。

`熟睡`的行为不变；主动`解除催眠`补上了对`苦痛快感化`的清除，与`熟睡`对齐。两个调用点各自原有的其他处理（无意识判定、空气催眠等）保持不变。

## 验证

用同一份存档，以凯尔希作为代表案例走同一操作流程：

- 操作前，凯尔希状态栏显示 `<催眠(200%):心控(敏感)(痛→快感)>`，同一画面可见`[4004]解除催眠`指令。

[![操作前：状态栏含 痛→快感，可见解除催眠指令]({{BEFORE_URL}})]({{BEFORE_URL}})

- 上游当前版本：执行`[4004]解除催眠`后，状态栏仍显示 `<催眠(200%)(痛→快感)>`，效果残留。

[![上游当前版本：解除催眠后 痛→快感 残留]({{BASELINE_AFTER_URL}})]({{BASELINE_AFTER_URL}})

- 本 PR：同样操作后，状态栏显示 `<催眠(200%)>`，`痛→快感`已随其他子状态一并解除。

[![本 PR：解除催眠后 痛→快感 一并解除]({{CANDIDATE_AFTER_URL}})]({{CANDIDATE_AFTER_URL}})
```

`必要修改对照`：

| 位置 | 当前远端 draft | 必须改为 | 原因 |
|------|--------------|---------|------|
| 标题 | 说`熟睡`错误解除了效果 | 说`解除催眠`未解除效果 | 语义完全翻转：`熟睡`路径现在是正确基准，缺陷路径是主动解除 |
| 问题 | `熟睡`清除是 bug，`解除催眠`保留是对照 | `解除催眠`残留是 bug，`熟睡`一并解除是对照 | 两条路径的正误角色互换，任何一句旧描述都与最终 diff 矛盾 |
| 原因 | 睡眠清理列表"意外多包含"了`苦痛快感化` | 直接解除的旧清理写于 `pain_as_pleasure` 字段加入之前，漏接了新字段 | 已确认根因不是多写一项，而是老代码没接新字段；因果方向相反 |
| 修复 | 共用函数清理**四项**，把`苦痛快感化`从睡眠清理中移出 | 共用函数清理**五项**（含`苦痛快感化`），直接解除补上清除 | helper 内容和行为变化方向都与旧稿相反；旧稿"解除催眠行为不变"在新 diff 下为假 |
| 验证 | 入睡流程截图，PR 侧显示效果**保留** | `[4004]解除催眠`流程新窄候选 A/B，PR 侧显示效果**解除** | 旧截图证明的是相反行为，不能声称支持当前 diff；占位符指向尚未上传的新图 |

不能只改一两句话的原因：四个小节的核心事实断言（哪条路径错、错在多还是少、helper 清几项、截图里哪一侧丢效果）全部是旧稿的镜像，保留任何一句旧断言都会与最终提交的 diff 或新证据图直接矛盾。四个标题和整体高度得以保留。

另：正文按要求未采用"否则玩家无法去掉"作为修复依据——修复依据是两条退出路径应结束同一组子状态、且列表只表达一次。

`publication_state: local-review-ready`
````
